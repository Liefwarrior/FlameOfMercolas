// Root build: shared identity only. Each module declares its own plugins,
// toolchain, and dependencies explicitly — no hidden cross-module magic.
allprojects {
    group = "com.trojia"
    version = "0.1.0-SNAPSHOT"
}
