# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IDE
.idea/
*.iml
out/
.vscode/
!.vscode/settings.json
!.vscode/launch.json

# OS
Thumbs.db
Desktop.ini
.DS_Store

# Java
*.class
hs_err_pid*
replay_pid*

# Local-only
*.log
/local/

# Native build artifacts published by `docker compose up --build`
/dist/

# Carrier-pigeon watch (local operational log, not project content)
/PIGEON-TO-CLAUDE.md
/PIGEON-FROM-CLAUDE.md
/.claude/worktrees/

# 1GB Kenney survey/extract pack — local-only reference, never committed.
# (The curated, tracked tiles live under content/art/kenney/CityPack/.)
/content/art/kenney-all-in-1/

# 3D BUILD. The Lord of Trojia asset lane's glTF exports (Synty POLYGON,
# Malbers Animations, weapons) -- licensed, so they live here gitignored with
# a committed manifest, never in public git. The game loads them at runtime
# when they are present; the tests never need them and draw procedural
# meshes instead.
/content/art/lot-3d/

# #77. The controls file the game writes beside itself. It is the PLAYER's,
# not the repo's: rebinding a key must not show up as a working-tree change.
granadad-controls.cfg
granadad-screenshot.png
