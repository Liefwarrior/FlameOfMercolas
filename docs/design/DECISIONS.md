# Locked Design Decisions — Granadad: The Darkstreets

Locked by Eli 2026-07-12 ("DECISIONS LOCKED — proceed to plan + build. Quality over speed, no shortcuts.")
Engine/sim decisions live in ARCHITECTURE.md; this file records the GAMEPLAY layer.

| # | Area | Decision |
|---|---|---|
| — | Title | **Granadad: The Darkstreets** — playing Gabri in Trojia, Ptolus-style urban dark fantasy |
| — | Canon | Read `..\LordOfTrojia-MVP` before inventing; placeholders clearly marked where the book is silent |
| — | MVP world | **ONE district: THE DOCKS** (Eli re-ruled 2026-07-12, rescinding a brief all-districts expansion the same day). The Docks + a 2-3 level starter dungeon beneath it is the entire playable MVP; the rest of Granadad is off-map narrative context (gates out, ships in, rumors). ptol.us referenced heavily for the Docks-archetype STRUCTURE (shop/NPC/street/building categories) — never names/text/content. Top-down tile grid, Moonring style. |
| — | Art register | **DF-translated Kenney: true-black void, real Kenney sprites, Roman-pillared civic facades** (Eli re-ruled 2026-07-15, FIFTH revision, verbatim: "The map looks very very ugly and you need to figure out how to make a beautiful design. You need to research and pull in some design skills that will allow you to create a properly realistic looking fantasy gritty city. Pick a muse, ask me if you need it, and stick with that feel for the whole city. Honestly, let's go for a completely black background so that you can use the best Kenney assets and specifically create complex architectural buildings. Trojia is very inspired by Rome, use pillars more. If you have something in your design memories that specifies it should look like crap remove it, you have good kenney assets now I want you to really use them. Roleplay as a senior gameplay and senior level designer while creating the city."). **Muse, confirmed with Eli, settled — do not re-ask**: Dwarf Fortress's own visual language, translated from ASCII glyphs to real Kenney sprite tiles — true black voids in unbuilt/unlit space, buildings as dense architectural masonry silhouettes (not thin outlines), warm torch/brazier light pools cutting through the black (a WorldRenderer additive-overlay pass over the existing light-tint pipeline, layered on the light-mask assets in `content/art/kenney-light-masks/`, flagged as a recommended fast-follow — not blocking this pass), and (the Rome cue) pillared/colonnaded facades on the district's grand civic buildings specifically, not applied uniformly. Kenney's 1-bit pack (`content/art/kenney/`, CC0) is the shipped default again — `ObserverApp.DEFAULT_ART_DIR = "kenney"` — superseding custom's MERCOLAS-24 pixel art. `voidColor` `#000000`, read dynamically from the mapping exactly as before (`ObserverApp.java` ~line 239) — no other rendering-path code change; this is a pure `art-mapping.json` edit per the M7 art-swap contract (TILE-ART-SPEC §8). Three new civic-only material ids — `granite_facade`, `brick_facade`, `reman_facade` (`content/raws/materials/*_facade.json`), each a physical clone of its base material minted via the `add-material-raw` skill — let `gen_docks_surface.py` tag *only* the street-facing frontage wall of the Weighhouse (K01), Mission of the Flame (K17), King's Bond (K12), and the Quayward Compound's (C1) gate wall with a genuine pedimented-colonnade sprite (real cells `(6,4)`/`(6,5)`/`(7,5)` of the same sheet); every other granite/brick/reman_concrete wall in the district stays plain masonry. Actor sprites (`SpriteIndex`) and `FaceGen` **keep the custom MERCOLAS-24 sheet, unchanged** — a deliberate call (bespoke tag-queryable system already tuned to this project's actor roster; the Kenney pack's own humanoid glyphs are a downgrade in expressiveness and don't map onto the named roster; no evidence saturated custom sprites read poorly against true black; `SpriteIndex`/`FaceGen`/inspector rendering are a separate parallel workflow's territory this pass stays out of), not a punt. `content/art/custom/` stays in-repo untouched as the `--art=custom` escape hatch, per this project's never-silently-delete convention. Prior superseded rulings, kept for history: ~~Custom original pixel art — "He Is Coming" palette, Earthbound detail, flat z-slice rendering (Eli re-ruled 2026-07-13, fourth revision)~~ · ~~Chrono Trigger faux-3D composite (third revision, 2026-07-13, rescinded same day)~~ · ~~Full-color Kenney (2026-07-13)~~ · ~~Luminous-on-black (2026-07-12)~~ | **Current mechanism:** the shipped default art pack is `kenney` (`ObserverApp.DEFAULT_ART_DIR`), the Kenney 1-Bit Pack v1.2 (CC0) through `content/art/kenney/art-mapping.json` (fifth revision) — material differentiation and per-position variant selection still follow the mechanism TILE-ART-SPEC.md §12 documents. `--art=dir` is the escape hatch back to `custom` or `placeholder` (`ObserverLauncher`'s own doc comment) — not the live default. The underlying light simulation is unchanged and still governs legibility: light-level dimming and z-peek dimming (TILE-ART-SPEC.md §5) still darken tiles away from light sources, as a multiply over each sprite's own baked colors — `lightTintQ8`/`zPeekDimQ8` are byte-identical to the fourth revision; only `voidColor` and the region/material vocabulary changed. Superseded rulings, kept for history: ~~Custom original pixel art (`tools/scripts/gen_custom_tiles.py`), MERCOLAS-24 palette, `DEFAULT_ART_DIR = "custom"` (fourth revision, 2026-07-13)~~ · ~~Tiles draw from Kenney 1-bit's pre-colored sheet variant, not the monochrome-tinted one — material differentiation comes from picking an appropriately-colored sprite per material (reusing the deterministic per-position variant-selection mechanism, TILE-ART-SPEC.md §12), not a tint multiply. First real art pass adapts the Kenney 1-bit pack (tintable monochrome) through the art-swap seam.~~ · ~~Luminous-on-black (Moonring style): deep black base, glowing color — the light sim is the star. (Eli, 2026-07-12)~~ |
| — | Companion | **Solo Gabri in the MVP**; Devin arrives post-MVP as a content beat. The combat screen's companion seam ships empty. (Eli, 2026-07-12) |
| — | Opening | **The game opens at THE DOCKS** — Gabri starts investigating there (Eli, 2026-07-12). Starter dungeon sits beneath it (smuggler cellars → sewer outfall → drowned crypt-edge structure). Harbor ward is canon-plausible (coastal peninsula capital) with specifics marked placeholder; investigation hook aligns with Gabri's canon breach-investigation arc (bloodletter sign inside Trojian territory, trail starts at the waterline). |
| 1a | Defense | **Hybrid per-part armor**: slots head/torso/arms/hands/legs/feet/shield/cloak/jewelry. All worn pieces contribute coverage-weighted to ONE aggregate AC used for hit/miss. On hit: roll body location; that location's piece mitigates damage and takes durability wear. Bare locations are weak spots. **Spec with worked examples + unit tests BEFORE implementation.** |
| 2b | Combat flow | **Dual-mode: top-down tile exploration, FIRST-PERSON combat screen** entered on encounter contact, decoupled from the exploration screen (Eli **amended 2026-07-28**, verbatim: *"2D tiles viewed from above moving like a Final Fantasy sprite, but when combat starts there can be a pause and a switch to 1st person for full controls. Best of both worlds with cool exploration AND sword and sorcery spells and steel combats."*). **This amends the PRESENTATION of 2b only — the flow it locked on 2026-07-12 is unchanged and was already correct**: dedicated screen, entered on encounter contact, sim engine paused for the duration (COMBAT-SCREEN-SPEC §1.2), `CombatEntryState` snapshot, return transition + consequences. What changes is the combat screen's CAMERA AND LAYOUT: ~~Skald-style side-vs-side rank layout~~ → first-person, for "full controls" in melee/spell exchanges. All resolution math in COMBAT-SPEC (aggregate AC, hit roll, body-location table, mitigation, wear, damage — 42 specced tests) is **untouched by this amendment**; a first-person camera changes what the player sees and how they aim, not how a blow resolves. Ranks/initiative/action-economy sections of COMBAT-SCREEN-SPEC need re-work to first-person framing; the transition, pause, log, morale, flee and return-consequence sections stand. Binding companion: **the tile sim remains authoritative in both modes** — see the Presentation-architecture row below. |
| — | Presentation architecture (binding, added 2026-07-28) | **The tile simulation is the model; every camera is a view over it.** Eli, emphatic and unprompted: *"do not ditch the tile sim... we need the tile sim in the background even if the player playing through it sees something 1st person."* Long-range target register (POST-simulation, not current work): *"Morrowind skills and systems with FO New Vegas factions with Daggerfall's graphics"* + *"faked 3D via clever use of 2D as Gabri sleuths about"* — where "Daggerfall's graphics" means its **billboarded sprites for everything alive**, NOT its raycast geometry. Why this is architecturally cheap rather than a rewrite: ArchUnit already forbids `sim-core` importing libGDX/`client-observer`, so the sim does not know it is being drawn; `client-observer` is *a* client, not *the* client, and a first-person combat screen (or a later exploration camera) is another view over the same deterministic tick stream — two views can never disagree, because the sim is the single source of truth. Everything load-bearing is grid logic a camera cannot disturb: 1-occupant-per-cell, A* pathing, z-links, sense radii, work reach (chebyshev 2), the S7 corridor-pinch predicate. `Actor.facing()` already exists (it prints on the character sheet) and Play mode already tracks a played actor, so a soul's-eye camera is largely position + facing. Depth Vision (S4, extended to actors in S5-6) already renders lower z-bands with scaled darkening and cool haze — a real working depth cue, and most of the way to convincing pseudo-3D with no polygons. **Rules that bind any future camera work:** (1) it is a NEW CLIENT MODULE or a client-side screen, NEVER a sim change; (2) the headless tile view stays the verification source of truth, so no rendered scene can quietly disagree with the sim; (3) it must not touch the S8-S12 economy arc — you cannot investigate an economy with no secrets in it, so the world that generates the mysteries gets built first. |
| — | First-person EXPLORATION camera (added 2026-07-29) | Eli, verbatim: *"I want a demo with the ability to switch between 1st person and the top-down tile view. I'm again envisioning something similar to Daggerfall where you can walk around and interact with the environment. It can still be tile-based movement and combat will still be in a battle screen eventually. Make sure you don't make it jarring, interpolate when moving so that you get a really smooth high fps if you need to. Like how in the Doom game the whole thing is technically 2D top down with trickery. I want that, but also with the ability to have a Z-axis like Daggerfall."* Ruled a **keepable foundation**, not a spike. **This does not touch 2b**: combat remains a separate paused first-person screen entered on encounter contact, and none of it is built here. Lives entirely in `com.trojia.client.fpv` + `FirstPersonInput`; `sim-core` untouched. **The technique**, chosen and defended in `FirstPersonPlanner`'s javadoc: a per-cell perspective projection with painter-ordered quads and billboards — 2D trickery in the Doom sense (no depth buffer, no mesh, no matrix stack; every surface is a textured quad in the existing `SpriteBatch`) but with a real pinhole projection instead of a column raycast. A Wolf3D/Doom raycaster was rejected because Doom could not put a room over a room and this ward is three walk planes with roof slums above and a flooded harbour below, and because its floors are a per-pixel pass no headless test can read; real 3D was rejected because visibility would live in the GPU. Accepted costs, all written down where they bite: no true pitch (looking up is a horizon shear), affine texture mapping across a quad, and one plan-view region on every wall face until an art pack authors a `face` form token. **Rulings the tile data could not make for itself, all pure rendering constants in `BandGeometry` with no sim meaning:** a z-band is **2.75 tile-widths** tall (a tile is one shoulder of standing room; a storey is about 2.75 of them — setting the band to one tile-width is the crawlspace failure this view is most often fudged into), eye height 1.70, a fluid surface sits `depth/8` up its own band (which puts full-depth harbour water just under the lip of the quay and a bathhouse pool ankle-deep, from one rule), and an unroofed column resolves to a **flat ambient-lit sky band** rather than to the art pack's void black, which read as a bug. **Binding on this and any later camera:** the movement seam is `PlayModeInput.applyMovement` and there is no second route into the sim's move intent; the view yaw is client-side and `Actor.setFacing` is never called; the vertical reach and the depth shade are `DepthVision`'s own, so the two views cannot disagree about how far down a column you can see or how recessed it looks; and the whole visibility decision is a GL-free planner returning records that name the cell each came from, so `FirstPersonAgreementTest` can stand on the real baked ward and diff the two views. **AMENDED 2026-07-31 — movement is CONTINUOUS, not tile-stepped.** After a frame-by-frame study of Barony (see `COMBAT-FEEL-REFERENCE.md`), Eli ruled verbatim: *"I want the continuous movement of Barony rather than Grimrock style."* This supersedes *"It can still be tile-based movement"* in the brief quoted above; nothing else in this row changes. **The binding consequence:** sim-core forbids float/double in state, and that rule is what makes the world hash cross-platform, so continuous movement is **sub-tile FIXED-POINT** — `(tile << 8) \| sub` in Q8, 256 steps across a tile — never floating point. It is specifically NOT solved by making the player's position client-only float state: that removes the player from the simulation, and the first time combat, occupancy, shoving or line-of-sight needs to know exactly where the player stands, the authoritative answer would live in the renderer. NPCs stay tile-stepped and interpolated at draw time (the existing stride-easing path); only the player takes sub-tile authority at first, and widening it later is a resolution change rather than an architecture change. The `STEP_ARRIVAL_FRACTION` / `strideEase` machinery exists to smooth discrete tile steps and is **replaced, not retuned**, for the player. Measured reference: Barony's keyboard turn is ~60-70 deg/s against our current 165 deg/s. **AMENDED 2026-08-04 — the C++ build shipped the crawlspace failure this row warned about, and the fix rounds 2.75 to 3.** `granadad`'s renderer drew a z-band as `z` to `z + 1.0` in the same units it used for a tile's width, with the eye at 205/256 = 0.80 of that: exactly the "one tile-width" band named above as the failure mode. Eli, playing it, verbatim: *"Definitely fix the roofs, all buildings only appear to be one tile high, I'd triple the amount of vertical height you're treating as '1 floor'."* **The number is now `kTilesPerBand = 3` in `native/include/granadad/sim/vertical_scale.hpp`**, with eye height 1.70 tiles unchanged from this row. Three rather than 2.75 for one reason that is specific to this codebase and does not apply to the Java one: the C++ body's vertical axis is Q8 integer and no rounding is allowed anywhere in it, and 3 is the only value in range for which every conversion is exact — a band is 768 Q8 and the eye's 435 Q8 divides by 3 with no remainder, where 2.75 gives 704 and a repeating 158.18. **Nobody should "restore" 2.75 later**: the two numbers are the same ruling and the difference is under the precision anybody can see. |
| 3a | Progression | Full Morrowind use-based skills — skills rise through use; this powers the weak→demigod systems-exploitation curve (north star) |
| 4c | Faces | Hand-authored text-art faces for named NPCs; a LITE version of Warsim: Realm of Aslona's layered-part face generator for generic NPCs, styled to our UI |

| — | Actors | **Streets of Rogue-style emergent agents** (Eli, 2026-07-12): many actor types with needs interacting in the simulated world, producing wild scenarios. Implementation mandate: an `Actor` base class extended by one subclass per type ("we can just keep adding on top" — add a type, watch what happens). Guardrails: subclasses stay thin (declare composed behavior policies + raws-driven stats); depth-2 hierarchy; all numbers in data files. Docks roster: Militia Watch, Serf, Wastrel, Priest of the Flame, Disciple of the Flame, Shopkeeper, Animal Keeper (always ≥1 Animal), Animal (always belongs to a Keeper). Spec before code (ACTORS-SPEC.md). This supersedes v0's "no agents" fence: actors become their own milestone right after F2. |

| — | Identity (FOR LATER — Play mode; seam required NOW) | (Eli, 2026-07-12) Actors carry **trueIdentity vs presentedIdentity** (Persona value object). PC builder gets `actAs()`, runtime `setActAs()` for disguises. ALL social systems (law, deference, faces, logs, witness reports) read the PRESENTED identity; disguise = voluntarily shedding Wielder immunity; Flame powers can pierce presented→true. Canon precedent: Gabri's deliberately plain features/hidden red hair; Bledhreft impersonating Senator Harris — the same mechanism powers demon impersonators, and the Flame's pierce is its counter. **Seam requirement for ACTORS-SPEC/F2.5: the Actor base's identity field must be Persona-shaped from day one** (two slots, presented defaulting to true); full disguise gameplay lands with Play mode. |

| — | Goals & Jobs | (Eli, 2026-07-12) **Every actor has a GOAL — a reason for being on-screen.** Goals come from a **Job taxonomy**: `Job` base class with hierarchical nested extension giving type-safe signatures — `Job.Serf`, `Job.Serf.Farmer`, `Job.FlameOfMerc`, `Job.Villain.Robber`, `Job.Villain.Cutpurse` — enumerated/bound from a .json file at startup. Reconciliation (per approved actor guardrails): Job classes define type identity + goal behavior; the JSON raws define data (spawn weights, goal parameters, schedules); startup binder validates the two 1:1, fail-fast on mismatch. Open for ACTORS-SPEC to resolve: exact Job↔Actor-subclass relationship (recommended: Actor subtype = what you are; Job = why you're here; secret jobs pair with presentedIdentity — a Cutpurse presents as a Wastrel). |

| — | Trojian housing: Compounds | (Eli, 2026-07-13) **Housing is organized into Compounds, not detached houses.** A Compound = one large walled lot: a courtyard/atrium farm at the center (crops for the resident families) surrounded by condo-like apartments or mansions belonging to owning families. Built up multiple stories where land is scarce; the ROOFTOP of a tall compound is walled off and rented out as mass low-income housing (tents/mud huts — informal, flammable, cheap). Wealth gradient IN MATERIALS: mansions/condos of owning families = **Reman engineering** (concrete-equivalent — Rema is the canon engineer-civilization); rooftop slum housing = cheap cloth/mudbrick/tent construction. Visual touchstone Eli gave: "the big crab in Ald-Ruhn" (Morrowind) — one large architectural shell containing a whole community's housing stratified by income, not a suburb of identical single-family lots. **Applies retroactively**: the "12 homes" of the city-block population effort become ~12 dwelling UNITS within one compound (a few condo/mansion units + several rooftop tent/hut units), not 12 separate buildings. **"Skyrunners"**: Granadad's in-world term for burglars/assassins, named because they break in through compound ceilings and flee across the rooftop-slum layer — a natural third `Job.Villain.*` alongside Robber/Cutpurse, tied to the Skyrunning skill and the Persona cover-identity seam. **Rooftops are socially unseemly** — being seen on one marks a Trojian as low-status or criminal; this is *why* the poor live there and *why* it's a villain highway. Retroactively explains BLESSING-QUEUE #6 (Skylining/Skyrunning kept Favored "for the Wielder") — Gabri's maxed social immunity exempts him alone from that stigma. Binding: no future rooftop-social mechanic may penalize a presented-Wielder actor. |

| — | The nemesis (S8) | (Eli, 2026-08-04) **Killing the player is a PROMOTION EVENT.** Verbatim: *"The player respawns when killed, but the person who killed them should rise in political rank and retain a rivalry with the player. Maybe the player could be killed by a laborer in a fist fight but next time the player returns that person might be promoted to a carpenter who's the head of a new Carpenter's Guild or something."* Binding on the implementation: the rise MOVES THROUGH THE SYSTEMS THE WARD ALREADY RUNS ON and never through a parallel one -- a rung off the owner's own `ranks.json` with the authored title, influence through the S4 mirror so the winner's guild gains what its declared rival loses, a trade house out of an authored file with REAL members and a permanent cut of real prices, and a vacant charge taken on the same compound roll section 2.8 says any actor may petition for. **Permanence is the point:** beating him afterwards takes the grudge down and leaves the rank, the house, the toll and the ground exactly where they are -- you can win the rematch, you cannot un-found his guild. **The PLAYER respawns as themselves**, per the 2026-07-31 ruling that the persistent-ward variant (the city surviving your death while you return as somebody else) is not being built from the start -- *"it's easy to change that later"* -- so every record is keyed on the WINNER's name and the day a save carries a second protagonist nothing in the book has to be rewritten. |

| — | Stealth, thievery, lockpicking (S9) | (implementation ruling, 2026-08-04, under Eli's Ardenfall brief -- *"Stealth, bartering, thievery, lock picking"*) **LIGHT AND SOUND CROSS THE LINE, IN INTEGERS.** `render/lighting.hpp` opened with "there is no stealth system", correctly: light was float, presentation, and read by nothing that could change the world. S9 does NOT make the simulation read the renderer and does NOT put floats in state -- it builds a SECOND, INTEGER field over the same authored lamp sources, on the same radius/peak/falloff curve, owned and hashed by the simulation. Two fields over one set of lamps is a real cost and it is the only shape that keeps both standing rules. **ONE NOTICE RULE, NOT TWO.** `witnessCount`, `spreadWitness` and the Watch's `canSeePlayer` all resolve through the same `noticeBy()`, with a `static_assert` holding `kWatchSightTiles` and `kWitnessRangeTiles` together, because a crime witnessed by somebody the heat never counted is exactly the defect S3 and S5 each fixed once already. **NO ROLLS IN THE SEEING OR THE THIEVING.** Both are integer comparisons, for the same reason the S3 pickpocket check has never rolled: a draw taken inside a const query is a hole in the twin-run gate. **A LOCK IS NEVER RE-ROLLED.** Its pins are a pure function of `(worldSeed, lockId, pin)` -- no tick, no stream position -- so a player may walk away from a half-picked box and come back to the same box, and a retry is not a fresh lottery. **SKILL BUYS INFORMATION AND FORGIVENESS, NEVER SUCCESS**, per the standing Morrowind steer: CRACKSMANSHIP unlocks the feel and the tolerance, and the search is still a search. **STEALTH MOVES A NUMBER, IT DOES NOT DECIDE A LIFT.** Whether a mark feels the hand stays CRACKSMANSHIP against their STREETWISE exactly as S3 resolved it; the light, the crowd and your posture move that number by a BOUNDED amount either way, so neither half can swamp the other. |

| — | Trojian tenure: the charge and the bond | (Eli, 2026-07-29) **Property in Granadad is a three-tier ground lease under a Church freehold, and Trojian serfdom is the debt that settles it — a relation between two actors with terms, not a caste and not a status flag.** Eli, verbatim: *"In lieu of paying Royals for rent you can lease yourself to your Den Duke (update lore to include this term, a Den Duke is someone who owns and rents property in Granadad.) This is also the same sort of system that a Craftlord might use to get workers in his shop, and how Guilds acquire competition."* Then, on the structure: *"The den dukes should own a parent plot that contains all houses within the compound. Den dukes own the compound while individuals own the houses."* Then, superseding any two-tier reading of the first: *"The plots should be owned by the priests of the flame for historical and legal preservation. Then dukes and tenants can make agreements to lease. A priest should be required to evict and they should consider the tenant too."* **THE THREE TIERS.** (1) The **Priests of the Flame** hold every PLOT in trust, for historical and legal preservation. The ground is not property; it is a **charge**, and it is never sold, only let. (2) A **DEN DUKE** holds the charge on a plot — the compound, its wall, its gate, its courtyard — pays the Flame a charge-rent, and lets the ground under each house to that house's owner. A **CRAFTLORD** is the identical instrument on working ground (a yard, a shed, a quay frontage): same table, different title. (3) **Individuals own their HOUSES outright**, standing on ground they will never own. A family can own its house free and clear and still owe the man who rents the earth beneath it, and that man does not own the earth either. **RENT IS GROUND RENT** — the *ground penny*, owed by each house-owner to the plot's Den Duke, never rent on the dwelling. **PAYABLE IN ROYALS OR IN YOURSELF:** a house-owner who cannot find the penny may go **bondsworn**, leasing their own labour to their Den Duke until the arrears are worked off. That is what Trojian serfdom is. A serf is not born a serf; a serf is a house-owner who had a bad quarter. **ROOFTOP RULING:** a rooftop lodger rents from the HOUSE-OWNER BENEATH THEM, not from the Den Duke — a roof is part of a house, and the house is individually owned. This makes every mansion-poor house-owner a petty landlord, and it means that when a house is taken for arrears the roof goes with it and the roof people were never parties to the case. The priest weighs the tenant; nobody weighs the lodgers. **EVICTION REQUIRES A PRIEST, AND THE PRIEST IS AN ARBITER.** A Den Duke cannot turn a family out; he must bring a **petition** to the Flame and the priest weighs the tenant as well as the arrears (see DOCKS-GAZETTEER §2.8 for the hearing, its inputs and its six outcomes — the outcomes are not yes and no). This completes a pattern already ruled: the Church is the ward's purse, approves trades as ordinary business, and sanctions the redemption of a scalp. One conscience now sits under trade, under blood money, and under the roof over every family's head. The Church never opposes anyone openly; it makes itself unavoidable while appearing to serve. **RETIRES A PLACEHOLDER:** DOCKS-GAZETTEER §4.2's SERF dossier framing ("'serf' framing placeholder") is superseded — the ward's serf mass is now defined by a MECHANISM (a debt relation dressed as a tenancy) rather than a caste label. This is a ruling, not a drift. **BINDING ON PRESENTATION:** ownership is never rendered. Eli, verbatim: *"no it doesn't need to be visible, just use the info to populate signage appropriately."* No ownership overlay, no painted ground, no boundary lines, no debug layer. Ownership is a fact the world knows and the SIGNS report; the player learns who holds the ground by reading the ward. **AMENDED 2026-09-02 — the duke's tenure is a COVENANT, and "charge" retires from player-facing speech.** Eli, verbatim: *"'DEN DUKE holds the charge' doesn't make any sense, just say that the DEN DUKE owns the land and makes a covenant with house-owners to build and farm on. They should be called 'covenants' since they're meant to go on indefinitely."* So in everyday speech: the Den Duke OWNS the land, and the duke↔house-owner agreement is a **covenant** — meant to run indefinitely, with the ground penny as its rent. **RESOLVED same day**, Eli verbatim: *"Yeah the Flame holds the plot in trust....FOR the Den Dukes. The mission has lawyers and administrators and keep safe all the covenants."* The trust runs in the dukes' favor — no legal fiction. The Mission is the ward's covenant registry: lawyers and administrators keeping every covenant safe under its roof. And the central registry has a name and an address beyond this ward — Eli, same day: *"The Hall of Covenants for example should be a future location in another district."* **THE HALL OF COVENANTS** is the first named future-district location: off-map narrative context for the Docks chapter, a real place to build when its district ships. **Clarified moments later**, Eli verbatim: *"Now the Flame still is the top dog, they just use the DEN DUKES so that they don't have to do the work of landlording the compounds."* So the trust-for-dukes is delegation, not surrender: the Flame remains supreme, and the Den Dukes are its working landlords — the layer that spares the Flame the labor of running compounds. And the working analogy, Eli verbatim: *"The DEN DUKES are like property management companies."* **Vocabulary blessed (Eli, 2026-09-02):** "ground penny" was an implementation-pass coinage, not Eli's own words; he ruled on it directly: *"I bless it, the ground penny is a fine name for rent."* It is canon vocabulary now, and player-facing material should gloss it plainly on first use (ground penny = rent). Player-facing copy says covenant, never charge; historical doc headings keep their names. |

| — | Crime/Arrest/Escalation (supersedes ACTORS-SPEC.md §2.7 item 5 for Skyrunners) | Eli re-ruled 2026-07-14 (verbatim): "guards should arrest criminals for 1-3 days before turning them loose unless they're a skyrunner then it's cut off their hand first offense and hanging on the second." This **supersedes, for Skyrunners specifically**, ACTORS-SPEC.md's earlier §2.7/item-5 ruling "the Watch arrests, never executes" — that hurt-not-kill rule stands unchanged for ordinary Robbers/Cutpurses (arrest → `HELD` → a drawn 1-3 day sentence → release, no execution path exists for them), but a Skyrunner's 2nd repeat offense is now a permanent `EXECUTED` status (hanged; the actor is never removed from `ActorRegistry` — no removal path exists — it stays resident, permanently inert). A Skyrunner's 1st offense is `MAIMED` only (cosmetic status bit; no stat penalty, since no combat system exists yet to attach one to). Implementation: `JobBehaviors.checkArrestExposure` (detection + arrest/escalation transition), `HeldPolicy`/`ExecutedPolicy` (new `PolicyId`s), `StatusBit.HELD/MAIMED/EXECUTED` (new bits 6-8), `Actor.heldUntilTick`/`offenseCount` (new scalar fields, the `goalProgress`/`goalWorkTicks` precedent). Both docs should be read together going forward: ACTORS-SPEC.md's "never executes" line is correct for Robber/Cutpurse, incorrect for Skyrunner's 2nd offense. |

| — | The brawl line (added 2026-08-04, S2) | **A fight resolves IN THE WORLD, with no screen transition, exactly while all three of these hold:** (B1) **nothing edged is out** — every hand in it holds fists, something improvised off a table, or a carried cudgel; a knife, a boat-hook or a cutlass is not a brawl weapon; (B2) **nobody means to kill** — intent is state, not a guess, and an actor who escalates to `Kill` has decided; (B3) **nobody is being finished** — a fighter at or under a quarter health is BLOODIED, and beating a bloodied man while meaning him `Harm` is not a bar fight any more, whatever is in your hands. The moment any one of them stops holding the fight is LETHAL and belongs to the dedicated first-person combat screen (2b). **B3 is what makes this a rule rather than an inventory check:** two men swinging stools at each other is a brawl however long it goes on, as long as neither has decided to do more than win — a Subdue beating that puts somebody on the floor is exactly what a bouncer does for a living. What tips it is intent meeting damage. **Corollary, ruled at the same time:** a brawl never kills the player; hit points floor at 1, the fight ends there and what follows is being carried out, not more fighting. Player knockout and everything downstream of it lands with the combat screen. Implemented in `native/include/granadad/sim/brawl.hpp` (`classifyFight`, `resolvesInWorld`) and tested as a table with no world anywhere near it; the tavern re-classifies every simulated second, so a brawl that starts with fists escalates the instant a blade comes out. This satisfies Eli's 2026-08-04 requirement: *"You will need a clear rule for which is which — make it explicit and testable."* |

| — | Doorways are not sealed by their own lintels (corrected 2026-08-04, S2) | Not a design ruling so much as a design ruling the code had accidentally made. `TileQuery::headroom` refused a **WALL** one level above a body while allowing a **FLOOR** there. Against the geometry the renderer draws from the same lanes that is backwards: a FLOOR at z+1 is the slab hanging from the top of that cell — the tightest ceiling there is — while a WALL at z+1 fills the cell from its own floor upward, a whole tile above the feet, which is *more* clearance. Every door in every two-storey building in the Docks has a wall above it, because the storey above has an exterior wall and the door is a gap in the storey below. Under the old rule the door tiles of the Gilded Gull, the Bilge, the Lantern Room and every other multi-storey site were not standable, so nothing — player or actor — could get through them: the interiors were authored, baked, rendered and unreachable. The only form that refuses headroom now is VOID, the world's own border ring. The district gained **1,606 reachable tiles**, all of it on the two bands that have buildings on them; the upper band and the strand below the quay did not move by a single tile, which is what says the change opened interiors and not the map. Pinned counts re-derived in `native/include/granadad/sim/docks.hpp` and re-checked from the baked bytes on every build. |

| — | Talking is a list of topics, and the list is a MAP (added 2026-08-04, S3) | **No dialogue topic is ever gated by a dice roll.** DOCKS-GAZETTEER §5.3 already ruled it — *"People TELL the Wielder things. The investigation is never persuasion — it is knowing WHERE to ask... No dialogue-skill checks exist; the gate is geographic and social-topological"* — and S3 is where that stopped being prose and became the shape of the code. A topic is on the list for exactly two reasons: the person in front of you is a PARTY to that story (`content/raws/names/histories.json`), or `content/raws/rumors/rumors.json` licenses them to REPEAT it. Nothing else puts one there and no roll can. Consequences that bind: a player who cannot get an answer has not failed a check, they are standing in front of the wrong person, and the fix is to find a better one; a topic list is therefore a MAP of the ward's knowledge and reading it is play. **The one exception, deliberate and narrow:** trade. Skill decides what a mug of ale costs, because trade is the one exchange where both parties are openly trying to get the better of each other. Implemented in `native/src/sim/dialogue.cpp` (`buildTopics`) and `native/src/sim/notables.cpp` (`tellableBy`); the reachability case is "who can tell you what is decided by the raws, not by a dice roll". |
| — | Standing is a number, but it is never SHOWN as one (added 2026-08-04, S3) | Reputation has to be readable in the world rather than in a stat screen, so a disposition moves three things a player can see without opening anything: **the words** — the greeting comes out of `greet.<family>.<attitude>.<band>` in the owner's authored tables, so a hostile docker at four in the morning is a different sentence and not a recoloured one; **the price** — what the bar and the stair charge is computed from standing and streetwise, so a bed moves from eight coin to seventeen across the range with nobody haggling at all; **the service** — a landlord who caught you with a hand in his till does not pour, and returns `Refused`. The ward's own opinion sits in the corner of the HUD as a phrase ("NOBODY IN PARTICULAR", "THE WARD WANTS YOU GONE"), never a number. **The rule that keeps it honest:** you cannot chat your way into being liked. Conversation alone tops out one point short of WARM (`kTalkCeiling`); everything above that costs coin spent, a favour done, or trouble not started. And a robbery in a full taproom is not a private arrangement — everybody within eight tiles remembers it and the ward hears. |
| — | Relationships are simulation state, not client state (added 2026-08-04, S3) | Every disposition, every count of favours and injuries, every skill the player has earned by use, and whatever price is currently on a counter is folded into the world hash the twin-run gate compares. A relationship the gate cannot see is a relationship the gate does not protect. Corollaries: selection of which authored line gets spoken is **draw-free** — a pure function of who you are talking to and how many times you have done it — because a conversation that consumed an RNG draw would shift the simulation's draw sequence and a run where the player talked would diverge from one where they did not; and `skipTo()`, which fakes the hours it skips, deliberately does not touch the ledger, so sleeping a night does not make anybody forget you robbed them. |

| — | Every rung is earned, and a rung is measured in what your hands can do (added 2026-08-04, S4) | **A faction has four numbers and not one more: membership, rank, standing and INFLUENCE.** Ranks are never granted for showing up — every rung, *including the first*, costs standing with that faction AND a level in that faction's own skill, which is the Morrowind steer turned into a gate (`content/raws/factions/ranks.json`, `FactionLedger::checkRung`). The five ladders are deliberately five different shapes — three rungs for the garrison and the Row, four for the roofs and the Mission — because "each with its own ranking system" is not one ladder with the labels swapped. **What a rung buys is a TOKEN read by name**, never a rank number, so re-ordering a ladder cannot silently move what a rung opens; `teaching`, `forge` and `price` are read this sprint and the rest are declared where they belong and read by nothing yet. **An actor's faction is DERIVED, not tabulated**: the room knows a bartender's job family and the owner's own `factions.json` says which faction claims that family's jobs, so nobody wrote a second table and nobody can let one drift. |
| — | Influence is what reaches out of the character sheet (added 2026-08-04, S4) | Standing is what a guild thinks of you; **influence is what the guild is worth in the ward**, and it is the number with hands. It moves two things a player feels without opening anything: **the price** — a member-run counter sells to one of its own at the member's rate and prices a stranger up by however much weight the guild has (`guildPricePercent`, folded into `HaggleTerms` so the counter and the haggle can never disagree); and **the rope** — when the Watch's influence falls and the Skyrunners' rises, the houses stop waiting for the Watch and a bouncer's grace shrinks from twenty seconds toward six (`Tavern::graceSecondsForPlayer`). **The mirror is canon's, not ours**: `factions.json`'s own note calls the Skyrunners "the mirror ledger of every justice event", so influence pushed toward one declared rival is pulled off the other and standing gained with one is half-lost with the other. The Temple has **no** rival on purpose — the tenure ruling above says the Church never opposes anyone openly. |
| — | The Flame's office is joinable; the Flame is not (added 2026-08-04, S4) | The Priest of the Flame line is the reference faction implementation and it obeys `MAGIC-CANON.md` §5.5 exactly: **what Father Maell teaches and helps you compose is the SOURCE off the public-issue shelf, never the Flame**, and he says so out loud in the authored line — *"This is not the Flame. The Flame is an office and a vigil and it is not mine to hand you."* The office is visible (robes, the Mission, a ladder you can climb to Acolyte) and the power is not. **Every in-world surface says SIX** (§5.4): the one line in the game that names the count says six and is confidently, articulately wrong, and a test greps every authored row for "seventh" / "seven powers" / "seven systems" and fails on a hit — a check rather than a promise, because the next content pass will not have read the dossier. |
| — | Spellcrafting is composition, and canon already priced it (added 2026-08-04, S4) | `content/raws/spells/spells.json` was authored as (axis × time-shape × magnitude × duration × target × range) precisely so effects could be composed the way Daggerfall's Mages Guild composes them, and S4 is the verb that shape was waiting for. **The cost model is `MAGIC-CANON.md` §2 transcribed** — 4 per tile of distance (L454), 1 per transfer point (L454), 20 for an unbridged link (L459), duration priced per dose for a trickle and per period for a hold (L454/L457), ±2 on any live attribute row (L98), and a vitality floor of 1 (L516/L567). **The pairing table is a canon argument and not a validation nicety**: heat and the body's own tuning are HELD axes and take `WHILE_ACTIVE` only, a wound is DELIVERED and takes `INSTANT` or `OVER_TIME` only, and the other five pairings are refused by name because they would load, charge full difficulty, report success and change nothing. A test runs the whole table against the owner's own eleven authored craftings, so if the rules and the shipped raws ever disagree the raws win and this goes red. **A forged crafting's id is a pure function of its shape**, so the same composition made twice is one crafting and a grimoire never depends on the order of play. |
| — | The topic list is paged, and every visible topic has a key on it (added 2026-08-04, S4) | S3 shipped a twelve-slot grid numbered `1`–`9` and then three rows whose number was a full stop, reachable only by arrow keys with nothing on screen saying so — and Master Venn already filled all twelve, so the next topic added to him would have vanished with no ellipsis. **The page is NINE, because those are the keys**, the tenth row is `0 MORE (2/3)` and `0` is bound to it, and a list of any length is completely addressable from the keyboard with no topic ever dropped. The cursor and the page move together, so `E` can never pick something that is not on screen. `--topic=N` is **one-based** now, matching the numbers printed beside the topics. |
| — | A witness needs the same floor, the range, and a line of sight (added 2026-08-04, S4) | S3's "everybody within eight tiles remembers it" was one clause of three. The S3 review widened the radius to a hundred thousand tiles and the whole gate stayed green, and the filter was 2D — a sleeper in a guest room directly above the taproom witnessed a robbery through the floor, and anyone in the snug witnessed one through the partition wall. **All three clauses are stated now and each has a case that goes red when it is removed**: within range (with a case that stands somebody just outside it and requires that they remember nothing), the same band, and a supercover ray asked of `TileQuery::lineOfSight`. **With one deliberate exception**: inside two tiles no sight line is needed, because the Gull's bar counter is authored as solid masonry — correctly, a body cannot walk through it — and a strict ray would make the bartender blind to the hand in her own till. A counter is waist high and a wall is not; the tile lanes cannot tell them apart, and two tiles of grace is the honest way to say so until they can. |

| — | The roofs are a road, and three verbs are the whole of it (added 2026-08-04, S5) | DOCKS-GAZETTEER §2.5 has said since the Compounds ruling that rooftops are the burglar's highway and §2.6 filed the roof planes' same-z isolation as deliberate *"until the law/economy layers learn to climb (S5+)"*. **S5 is the sprint that learns, and it learns with three moves and no fourth.** **MANTLE** — haul yourself one band onto the top of the wall you are FACING, and the rule has three clauses that each refuse something real: a solid face to grip (or it is levitation), a standable surface on top of it (or you are climbing into masonry), and headroom over your own head (or you are standing up into a floor slab). ONE band, never two, so a two-storey frontage cannot be climbed from the street. **LEAP** — line the gap up and cross it, in the air, over `kLeapStepsPerTile` steps a tile on an integer arc; it aims at the FAR ROOF first and only takes the alley when there is no far roof, because a nearest-landing search makes every leap a fall and the roofs never join up. **DROP** — off the ledge in front, to the first floor under it, at most three bands. **The facing is snapped to the four-point compass** for all three: "which cell am I facing" has no answer at 47 degrees, and a body that half-committed would climb through the corner between two buildings. **What a skill and a guild buy is REACH and a SAFE HEIGHT, never a chance** — `skyrunning` and the Skyrunners' `roof` token each add a band of safe drop and a tile of carry, and nothing anywhere in the three verbs rolls a die. **The result, re-derived from the baked bytes on every build:** reachable-from-spawn goes 17,054 → 24,960, every standable cell of the mid-slope band opens, and the roof-slum plane at world z22 goes from **zero** reachable cells to 1,664. **Two bounds are stated out loud rather than discovered later:** a fall passes through AIR and nothing else (not through a wall, and not into the harbour — swimming does not exist, so falling into water is a refusal), and the caller declares the world's FLOOR (`docks::kLandingFloor`), because below the harbour surface the map is unbuilt dungeon and a body that fell into one of the 313 dry shafts in the harbour bed could not climb back out. A trapdoor into an unfinished level is not a feature. |
| — | Six crimes, one call site (added 2026-08-04, S5) | S4 shipped one crime — a hand in a purse — with no consequence outside the room it happened in. **The ward has six now** — lift, burgle, smuggle, fence, extort, roof-run — and every one of them, wherever it happens, goes through `DialogueDirector::noteCrime` and moves all four things an act should move: **the tally** a questline counts, **the heat** the Watch keeps, **the roofs' standing**, and — through the mirror `ranks.json` has declared since S4 — **the garrison's**. An act that reached one of those and missed the others is the bug that shape exists to prevent, and it is the first thing in this build that moves a faction number with no conversation open, which is what makes the mirror something a player can feel. **HEAT IS WHAT THE WATCH HEARD, NOT WHAT YOU DID:** an act nobody witnessed raises none of it, it cools five minutes a point (including through a night asleep in a rented bed, charged against elapsed seconds and not against a clock that wraps at midnight), and past sixty there is paper out on you. **The warrant has hysteresis on purpose** — issued at sixty, lapsed at twenty — because one cooled point should not flicker the state of being wanted. **Coin is coin and everything else is a THING:** a lifted purse pays out at once, a cracked box and a leaned-on trader hand you property, and property has to be sold to somebody. That somebody is a fence, and being a Skyrunner is not enough to be one — a cutpurse is not a fence, which is the whole of what the guild's second rung is worth. |
| — | Four dead unlock tokens get readers, and `grace` stops meaning two things (added 2026-08-04, S5) | The S4 review's finding was that eleven of fourteen unlock tokens had no consumer and that `ranks.json` was on its way to being a design document pretending to be data. Four are wired here and each buys a MECHANIC rather than a topic: **`roof`** (Skyrunner rung 1) adds a band of safe drop and a tile of leap — the guild's teaching is physical, which is the only kind of teaching the roofs have; **`fence`** (rung 2) is what makes the man in the snug buy what is not yours to sell; **`lair`** (rung 4) buys GOING TO GROUND — heat to nothing and the paper with it, because a brotherhood with a roost has somewhere to be for a week; **`warrant`** (Watch rung 3) buys LOSING THE FILE — the paper goes and the heat stays, because the ward remembers what the roll has forgotten. **And `grace` now means what it says.** The S4 review found the trap in it: the bouncer's grace-seconds read INFLUENCE, the ward's balance of power, which has nothing whatever to do with the token that happens to share the word. A watchman's rung adds eight seconds of rope on its own, and a warrant takes ten away, and those two are the token doing its own work. `gang`, `call`, `credit`, `charge`, `bunk`, `muster` and `teaching`'s neighbours remain declared and unread, and are still marked as such. |
| — | The Watch drinks in the Gull after nine (added 2026-08-04, S5) | The S4 review found that one of the five factions could not be JOINED IN PLAY — the Watch had no recruiter anywhere a player could stand, and its ladder, rivalry and influence were all proved through the sim API rather than through a conversation. **Watchman Cull is canon** (`notables.json`, `cull`, the impound keeper at K02) and the impound yard is a short walk from the Gull's door; a watchman having a drink after his shift in the captains' tavern is the least strained way to put the garrison within reach. It closes two things at once: the Watch is joinable, and `enemyPresence()` — which has counted rivals since S4 with nothing in the room to count — now has bodies, because the Skyrunners' contact and the Watch's recruiter drink in the same taproom between ten and one. He is also the pair of eyes a smuggling run has to get past. **And the man in the snug has his own name back:** S2 invented "Wisp Low-Tide" out of the authored wastrel pools because the Skyrunners had no line to hang anything on. The owner had already named him — `finch`, "the quiet tenant", sited at LAIR_SKYRUNNER — and `ranks.json`'s own note says the roofs' first rung is called Tenant BECAUSE of that epithet. Renaming him gave the line a personal bark table and an authored micro-history that an invented name could never reach. |
| — | A body falls through air and through nothing else (corrected 2026-08-04, S5) | Two geometry bugs of the same family as S2's lintel correction, both found by trying to WALK a scripted capture rather than by reading code. **(1) The Gilded Gull's guest floor has never been reachable on foot.** Its stair is one authored cell — a STAIR at z19 and a STAIR again at z20 — with open floor all round it on both levels, and the step rule prefers the same band, so every neighbour of the stair keeps a body downstairs forever; a flood fill finds ZERO transitions between the two floors anywhere in the building. S2 shipped `rentRoom()` and `sleep()` and S4 wrote a case about a robbery up there, and every one of them put the body on that floor by constructing it there. The sprint's own up-key takes the flight now, and a case asserts both halves. **(2) A body walked into an upstairs wall and fell through it.** The down-clause of the step rule read *"not standable at my band, standable one below, so step down"* without asking whether the thing at my band was AIR — and every interior partition of that guest floor is a wall standing over open taproom. 429 places in the district. Reachable-from-spawn goes 16,934 → 17,054 and every one of the 120 is on the upper band, which is what says the fix stopped a FALL rather than opening a door. **The rule, stated once for everything that moves vertically:** you fall through air; a wall, a floor and knee-deep water all refuse. |
| — | A scripted capture that fell short must fail (added 2026-08-04, S5) | The S4 review found `--flame` returning a stage count that `runSmoke` threw on the floor: a run that landed **zero of six** stages photographed the wrong person and exited 0. **A capture tool that reports success while photographing the wrong thing will mislabel a future sprint's evidence**, so a short run now says how short in the summary, says it again on its own line, and exits non-zero — and it still writes the PNG, because a frame of a run that fell short is evidence OF the shortfall and deleting it makes the failure harder to diagnose. The harness also walks with the room's OWN pathfinder now instead of a worse copy of one; the old walker's comment said it was "deliberately NOT a pathfinder" because RegionPath "belongs to the actors", and the result was that `--flame` only ever worked from inside the room. **And a counted questline stage now counts.** `tally` was added beside `alms` and the guard in `choose()` still named only `alms`, so a nine-stage line could be finished by pressing one key nine times; the scripted run found it. Turning a beat in requires having done it, which is what makes the scripted run a playthrough — go, do the one thing, come back and say so — rather than a keypress sequence. |
| — | Radiant work is generated in SHAPE and authored in every proper noun (added 2026-08-04, S6) | The sprint was asked for contracts that are "generated, repeatable and varied, but must not feel like slot-machine filler — each contract should reference real places, real named NPCs and the real economy." Those pull opposite ways, and the answer is a line drawn through the middle of them. **The SHAPE is drawn**: which broker is offering, which of the Forty wants it, whose ground it comes off, how many, by which night and for how much, all from the counter chain bound to `(worldSeed, day)` — so the same night of the same world always offers the same four jobs on any machine, whether or not the player was ever in the room. **Every NAME is authored**: a broker, patron or source that `content/raws/names/notables.json` does not have is refused at load, by name, and an offer left with no patron or no source is dropped whole. A brief is a template out of the new `content/raws/contracts/contracts.json` filled from the notables' own file, so a job always says who wants it, where they are, who lands it and where THEY are. **And the pay is the ward's own economy** — priced through exactly the `guildPricePercent` a mug of ale moves by, so climbing a ladder makes the work pay better in the units that already make drink cheaper. **A contract is not a questline and never becomes one:** `QuestBook` owns authored, ordered stages with an end; this owns work that repeats forever. The Skyrunner line GRADUATES into it — its last stage makes you a Cutpurse, and a Cutpurse is somebody Finch will give a job to. **Three brokers, three gates, deliberately a gradient**: the Watch's bounty is public, the landlord has to not dislike you, and the roofs want your name on the roll. |
| — | The Watch arrests, and the sentence is the one Eli already gave (added 2026-08-04, S6) | S5 shipped a warrant whose own header admitted "NOTHING ARRESTS", and the S5 review agreed: its only readers were a bouncer's timer and a menace sum. A law with no consequence makes every crime free. **A watchman now acts on two independent things.** A **LOAD**, noticed against his own kit-keeping and your streetwise and quoted in DRAMS rather than in units — eight jars of spirit are conspicuous and eight twists of dust are not, which is what makes weight the risk dial of the whole contraband layer. And **PAPER**, which is explicitly *not* a beacon: an off-duty man with a drink in his hand has to connect the face, and one glance in twenty-two does. That second number was TUNED and the tuning is on the record — at eight times the rate, a wanted man could not cross the taproom the only door is on, and the build's own scripted Skyrunner line was arrested on the way back from its last delivery. **Out of his sight is out of it**, and he gives a chase up after twelve seconds: that is the counterplay, and it is what the roofs are for. **The sentence is canon's, unchanged**: DECISIONS' own 2026-07-14 ruling — one to three days for anybody, the hand for a Skyrunner's first offence, the rope for the second — with a search that found no paper behind it costing a seizure and a charge and no cell. A maimed hand permanently takes half of what two hands take, the only lasting statistical penalty in this build. **NOTHING KILLS THE PLAYER:** `Sentence::Condemned` is a status the ward and the HUD both know about and no death is simulated, which is the honest state of a build with no combat screen. **And an arrest costs more than the night** — every taken contract whose goods went to the impound dies with them. |
| — | A bale has contents, and a scalp is redeemed under the Flame (added 2026-08-04, S6) | Two things the contraband layer settles. **The sack is real.** S5's bale was a bool and said so; there are five goods now — scalps, dust, quayfire, flower and a piece with somebody's name on it — each with a worth, a heat, a weight and the skill handling it trains, and a sack bounded by 240 drams or 48 units. A bale carried out of a door with no job open still pays the flat runner's fee to the boat's own buyer, exactly as in S5; carried out with a job open it comes off your shoulder into your own and the contract is what pays, because being paid twice for one bale is a faucet with extra steps. **And the Church signs for blood money.** The tenure ruling already says the Flame "sanctions the redemption of a scalp", so it does: a priest signs for the TAKING, before the knife rather than after it — the only reading a player can act on, since Father Maell goes home at half past nine and the rats do not come out until eleven, and the more honest one, because a sanction obtained afterwards would be an absolution. That leaves exactly a quarter of an hour in the day when the ward will both sell you the bounty and sign for it, and that window IS the shape of the job. Vermin only, which is where the Java build's own cull verb left it: human scalps stay deferred. |
| — | UI transition and legibility conventions (added 2026-08-11, Planning) | Two Hardening/Innovation sprints (task #83, task #85) fixed a specific, pre-diagnosed list of hard-snapping panels and unreadable overlaid text; a Planning-sprint sweep confirmed the pattern they landed on is the right one and found (and fixed, in the same shape) a few rows they had not yet reached — standingLabel/heatLabel/stashLabel, which still popped on `conversing` while their neighbour stealthLabel already eased. **That pattern is now settled convention, not a one-sprint style, and the next UI change is checked against these four rules:** **(1) EVERY UI ELEMENT THAT CAN APPEAR OR DISAPPEAR GETS ITS OWN `render::EasedToggle`, never a toggle shared across rows.** hud.hpp's eight bottom-band rows and its top-right stack (now eleven rows total) each carry their own `*Anim_`/`*Fade` pair for the documented reason: an interact prompt and a rung earned change on completely unrelated triggers, and a shared toggle would restart one row's fade every time an unrelated row changed. A hand-built state that never sets a `*Fade` field gets `1.0`, so this costs an existing caller nothing. **(2) TEXT DRAWN OVER A BUSY OR UNPREDICTABLE BACKGROUND GETS A `drawTextPlate()` BACKING, never bare text on the engine's generic 1px drop shadow.** The alert row and world-space signage (hud.cpp, signage_renderer.cpp) both needed this after a real capture showed unreadable text over a similarly-toned wall; a plate is a hard-edged solid field plus a flat bone border, drawn fill-then-border-then-text so the border is never eaten, sized to the row's own glyph box rather than the older pop-up's wider margins when several can be on screen at once (signage). Text over a fixed, known-dark or known-light surface (a panel's own near-black fill, drawn immediately before it) does not need one — the plate exists for text whose backdrop the layout cannot predict. **(3) IMPACT OR EVENT FEEDBACK USES `render::ImpactPulse` AND STAYS RESTRAINED — short duration, low peak magnitude, no looping — matching the actual numbers this build shipped with, not a vague aspiration.** `ImpactPulse` is an EVENT (`trigger()` jumps to full strength once, at an instant, and decays with nothing holding it open), never an `EasedToggle` flipped on and back off by hand. Restrained means: decay on the order of a tenth of a second at 60 steps/second (`decaySteps` around 8, the shipped default), and a peak visual effect well under a fifth of full strength (the brawl flashes blend at `0.16F`/`0.18F` alpha, the alert plate's overshoot is a couple of pixels of padding) — this is punctuation for a moody, text-forward investigation game, not a fighting game's hit spark, and a caller reaching for a bigger number should read that as a sign the moment does not belong on this primitive. **(4) CONTINUOUS STATE DRIVES CONTINUOUS VISUAL PROPERTIES — colour, size — NEVER A FIXED VALUE WITH A HIDDEN THRESHOLD.** `healthColor()` is the reference: a three-stop gradient off the same `clamped/maxHealth` fraction that already sizes the segmented bar, so the colour and the segment count can never disagree, in place of one fixed red that read identically at full health and at the brink. The Menu's own focus swap (`kMenuFocusRiseFallSteps`) applies the same rule to a state that is nominally binary (focused/not) by driving border colour AND thickness off the continuous `focusAmount` an `EasedToggle` produces, rather than switching both on the exact step focus moved. Implemented across `native/include/granadad/render/anim.hpp` (`EasedToggle`, `ImpactPulse`), `hud.hpp`/`hud.cpp` (`drawTextPlate`, `healthColor`, every `*Fade` field), `menu_view.cpp` (`drawPanelFrame`'s `focusAmount`), and proven reproducibly rather than merely argued: `--refocus=TILE`/`--refocus-steps=N` (session.hpp `SmokeRunConfig::refocus`) extends `--settle-steps` so a real, honestly mid-transition frame of rule (1) can be captured and reviewed on demand instead of only ever showing a cold start or a settled end state. |
| — | Rollout: districts as chapters (added 2026-09-02) | **The city ships one district at a time, as chapters — "all expansions", no base game.** Eli, verbatim: *"We can plan to do the city in districts or chapters, that can be our rollout style. Think of ES5 Skyrim, only instead of having a Base Game with Expansions ours will be all expansions or chapters. The cases should all lead you to finding the agent of evil that is the source of the unrest in the area. Big climax, then the player will be allowed to travel to another district (doesn't exist yet but that'll be the goal)."* Same day, the villain structure, verbatim: *"I want each district to have its own agent of evil with the Bloodletter being the leader of them all."* **Composition with standing rulings:** the 2026-07-12 "MVP world: ONE district" ruling stands — the Docks is chapter one, not the whole game. The opening investigation (casebook case THE BLOODLETTER, close line "SOMETHING CROSSED THE SEA INTO TROJIAN TERRITORY") is already shaped as the Docks chapter's agent-of-evil arc; under the new hierarchy, whether the thing at the heart of the Docks is the Bloodletter itself or its local agent is **deliberately unresolved — [NEEDS: Eli ruling] before the Docks climax is authored**. Binding on content: every case in a district pulls toward that district's agent; the chapter ends in a big climax; end-of-chapter travel to the next district is the long-term goal (no second district exists yet). **Diction rule, same day, verbatim:** *"Very few people know what a bloodletter is so only use that language with priests or well-studied people."* Binding on all dialogue and player-facing copy: common folk name the thing by its effects or by rumor, never by the term; "bloodletter" belongs in the mouths of priests and the well-studied. Existing copy (the casebook title, letters, street rumor) should be audited against this rule — flag violations, do not silently rewrite. |
| — | Combat: Oblivion-inspired action (added 2026-09-02) | **The JRPG-style combat register is SCRAPPED; combat is real-time first-person action, in-world.** Eli, verbatim: *"combat, entirely scrap the JRPG vibe of it and replace it with a combat system heavily inspired by Oblivion, where you have a dedicated button for your spells and a swing/block/hold to swing hard action combat for now."* This supersedes the "separate paused first-person combat screen (2b)" register wherever the two conflict — no screen transition, no turn/menu vibe. The model: a dedicated SPELL button; SWING; BLOCK; HOLD-to-swing-HARD. "For now" is in the ruling — this is the v1 action model, not the final ceiling. **Open questions flagged, not resolved:** how the brawl line's classification (legal/social layer — fists vs steel, Subdue vs Kill, Watch response) composes with in-world lethal combat now that there is no separate screen (the classification law reads as SURVIVING as the legality layer; the screen register is what dies — confirm with Eli); how real-time swings stay deterministic under the integer-tick law (the demo's one-sim-step-per-rendered-frame precedent is the likely spine); what "scrap" removes of COMBAT-SCREEN-SPEC and its tests. A scout+design program is chartered to answer these before a line of build. |
| — | Combat build landed: action combat v1 (added 2026-09-10) | **The action-combat model of DECISIONS:63 is BUILT.** The approved spec plus Eli's veto pass (2026-09-02) are assembled in `COMBAT-ACTION-SPEC.md`, which is the build contract. The four binding veto rulings the build follows: **(1) TARGETING IS A SIGHTLINE RAYCAST, NOT A CONE** — a player swing (and touch-cast) hits the first body on the look-ray down the crosshair (draw-free integer projection off `angle.hpp`'s `forward_x_q16/forward_y_q16`; on-line iff `0 < along ≤ kMeleeReach && \|perp\| ≤ kBodyHalfWidth`, target = smallest `along`); `kBodyHalfWidth` is a DISTANCE (128 Q8), never an angle; the fist's rat-vs-person species preference is retired for player swings, and friendly fire is real and the player's aim's fault. The draft's 120° cone is dead. **(2) EVERYONE CAN DIE**, including case-critical notables — no plot armor, no classifier shield; a case whose party is dead parks with an honest casebook line. **(3) INTENT-BY-VERB** — no intent menu; default Subdue, the first HARD swing in a fight sets Harm, and hard-swinging a bloodied man trips B3 → Lethal bare-handed. **(4) The Daggerfall JUSTICE system (court/jail/execution = game over) is a SEPARATE build AFTER this one** — combat v1 lands ONLY the crime + heat + arrest + `Sentence::Condemned` hook; no court, trial, or execution here. What v1 built: `strike()` extended by a trailing `chargeQ8` (256 default, bit-identical; 512 hard doubles rolled damage); the sightline raycast; the player state machine (`stepPlayerCombat`: IDLE/CHARGING/RECOVERY, `playerAttackDown/Up`, the named step constants) with the winded-refuses-hard rule; per-step NPC swing cadence (`stepBrawl`, re-keyed to a per-actor `npcSwingSeq_`) — THE ONE declared tavern/gate-workload baseline move; the two new wind costs; `FightClass::Lethal` selecting RULES not a venue (blows kill, floors lifted); `Activity::Dead` + the corpse conventions (a Downed that never stands, never removed, skipped by nearestTo/schedules); `kMurderHeat`/`Deed::Slew`/the three-clause witness rule → heat + the `Sentence::Condemned` arrest hook; the deference gate (Watch never hostile to a presented Wielder). The population baseline `0x2646C1AAA2BA38DF` did NOT move (no WardActor touched). Reviewed on `combat/build` before it merges to wip. |
| — | Oblivion feel, the Gull: fighting mode, the room fights back, the Watch on seen violence, the rhythm (added 2026-09-10) | **The one declared tavern/gate-workload baseline move of the Oblivion-feel program is LANDED and RE-BLESSED ONCE: `0x0D83D18F00E2C6CE` (a21dc3e, combat v1) -> `0x86E05F527E54E795` (feel/build), the tavern twin `granadad-twin-gate --tavern --ticks 900`, run A == run B.** Eli, 2026-09-10, verbatim: *"When I press LMB I want to enter fighting mode and hit whoever is in front of me. I'd expect the watch and crowd to react appropriately to the violence they're witnessing."* Four things moved the hash, in two lanes, folded into one declaration so the number is re-blessed once: **(1) FIGHTING MODE** -- `handsUp_` + `lowerTimer_` on `sim::Tavern`, hashed, drawing nothing: hands UP on the first Attack press from idle (the same press swings: tap = raise+swing, hold = raise+hard), on the guard, on a caught blow; DOWN on USE with nothing in reach (LOWER HANDS, a new interact slot), after `kLowerHandsSteps` (600) of lull, and on the cancel list (talk, page, pick, sleep, arrest, defeat). The stance is what the world reads. **(2) THE ROOM FIGHTS BACK** -- NPC blows resolve under LETHAL rules with the floor lifted to zero; an NPC blow kills the player through `applyDefeat` and the shipped death ceremony; the brawl floor holds at one exactly as shipped. **(3) THE WATCH ON SEEN VIOLENCE** -- `WatchCause::Violence`: a watchman who can SEE the player (the three-clause notice rule, `canSeePlayer`) during a live lethal-class fight, or with steel UP in the player's hands, or the player's hands up over a corpse, goes Closing at once (no glance gate, no notice die) with the `watch.halt` bark (falls through to `watch.demand` until the row is authored) and ARRESTS AT REACH exactly as the contraband path does (`applyArrest`, the report names the cause). NEVER for a brawl-class fist fight (a bar fight is the house's law) and never with the hands down over a body somebody else left. Deference to a presented Wielder stands absolute. A player blow on the CLOSING watchman makes him a brawler with `Intent::Kill` (Lethal by B2; he fights, he does not arrest, and his blows can kill and rise a nemesis); killing him is MURDER through `slayActor`, never "resisting". A corpse cannot witness (`noticeBy` now reads `isFloored`). **(4) THE RHYTHM** (all integer, all hashed on `Actor` / `Tavern`, no new draw): every NPC blow is TELEGRAPHED -- `kNpcWindupSteps` (18) or `kNpcHardWindupSteps` (30) of wind-up inside the unchanged 66-step interval, the roll drawn at the wind-up and kept (`npcPendingRoll_`), the swing COMMITTED (it lands only if the man is in reach on the landing step; stepping back makes it whiff, never cancels it); a player hit inside the wind-up, or any hard hit, STAGGERS him `kStaggerSteps` (24) and the wind-up is lost; his GUARD is a band off his OWN swing roll (bits 40-47: `kNpcGuardBand256` 48/256 for a patron, `kNpcProfessionalGuardBand256` 128/256 for a bouncer or watchman) and a HARD NPC swing is a second band (bits 48-55, `kNpcHardBand256` 40/256, double rolled damage, the longer tell) -- same-roll discipline, one draw per swing; a tap into a raised guard is softened (`blockedDamage` at level 0, no NPC shieldwall ledger -- a second table, so not trained) and RECOILS the arm `kRecoilSteps` (60, longer than the 36-step recovery on purpose), a hard swing into it BREAKS it (`kBlockStaggerSteps` 30); the player's held guard catching a HARD swing is softened once and then BROKEN for `kBlockStaggerSteps` (press refused, guard not honoured) -- the Oblivion asymmetry; PATRONS STAND BACK on the escalation edge (`kStandBackRadiusTiles` 3 / `kStandBackTiles` 2, directly away, first standable tile inside the footprint) and the rota holds the whole room while the lethal fight is live; BOUNCERS REFUSE STEEL (no ejection ladder into a lethal fight -- the responder holds the door tile, `kDoorX0, kDoorY+1`, and the ladder stands still); ROUT (a bloodied non-professional -- not a bouncer, not the Watch, not a Kill-intent nemesis -- breaks off for `gull::kStreetX/Y` under lethal rules, purposeful, leaves `brawlers_` on arrival, keeps the street until the fight ends); a defeat resets intent-by-verb to Subdue like every other fight end. This LIFTS the v1 ceiling line "no NPC blocking, no NPC hard swings" (COMBAT-ACTION-SPEC.md section 10) for the TAVERN ROSTER ONLY, by the constants named; NPC-vs-NPC lethality stays on the ceiling. The population baseline `0x2646C1AAA2BA38DF` did NOT move (no WardActor touched; confirmed on the gated exe). Bark row `watch.halt` is referenced by key and falls through to `watch.demand` until the owner-canon row is authored (the Barks lane); `brawl.join` / `crowd.flee` are that lane's too. The viewmodel is the 3D build's; the street reaction (9a/9b/9c) is its own declared population move after the justice build. |
| — | Oblivion feel: street panic — the ONE declared population baseline move (added 2026-09-10) | **9a STREET PANIC IS BUILT, and the population baseline is DECLARED MOVED and RE-BLESSED ONCE: `0x2646C1AAA2BA38DF` -> `0x2646C1AAA2BA38DF` (feel/build), `granadad-twin-gate --population --population-hour 16 --ticks 7200`, run A == run B, 18,772-byte report — the number did not change and the declaration stands regardless, because the law is about WardActor BEHAVIOUR moving off the player and not about the arithmetic.** Eli, 2026-09-10, verbatim: *"I'd expect the watch and crowd to react appropriately to the violence they're witnessing."* The gap analysis' finding: `WardPolicy::Flee` was built, hashed and DEAD for people — every authored actor row starts Safety at 9,000-10,000 with decay 0, and the only thing that ever lowered it was a cat closing on a mouse. **What landed:** `WardPopulation::alarm(x, y, band, radiusTiles, severity)` frightens every PERSON who can SEE the event — the three-clause witness rule the Gull's `spreadWitness` keeps, draw-free: SAME BAND, WITHIN RANGE (Chebyshev, `witnessesAround`'s own metric), LINE OF SIGHT (`TileQuery::lineOfSight`, so the roofed and walled Gull reaches the street only through its door) — by driving Safety under `kNeedCritical`, so FLEE fires at the raws' own priority the next tick and the street scatters through the flee step that already existed. **Three tiers** (`AlarmSeverity`, every constant in ward_actors.hpp with its reason): STEEL (a blade in a raised hand, or hands up over a floored man in reach) = radius 6, floor 700, ~half a minute; BLOW (a blow landed under lethal rules) = radius 12, floor 250, ~80 s; KILL (a non-vermin corpse appeared) = radius 24, floor 0, ~105 s. **Recovery** rides the existing needs accumulator at `kPanicRecoverPerTick` (9/tick) — a CODE constant, not raws (the raws' own 2-4 per Java tick rescales to about half a point a second, a half-hour panic; the raws when the actor files are next opened) — applied ONLY while a person is under the FLEE gate, so the un-alarmed arithmetic is byte-for-byte what it was. **THE WATCH IS EXEMPT** (it holds where the crowd runs — a watchman fleeing a knife is the wrong feel; Respond is 9b, after the justice build); beasts are not asked (the mouse's own rate is tuned to its chase). **The ONE client call site:** `Session::step` reads the tavern's public state by the comparison-not-flag shape the hp watch uses (`lastRoomHpForAlarm_` / `lastCorpsesForAlarm_`, unhashed): a corpse appearing = KILL (the edge, at once); the room's non-vermin hp falling while `lethalFightLive() \|\| escalated()` = BLOW (the edge, at once); `playerHandsUp()` with steel or over a floored man = STEEL, re-asserted once a simulated second so the bubble follows a man walking the Tarwalk with a blade out. A bar-fight punch frightens nobody outside. **No new hashed field anywhere** — needs[3] and needAccum[3] were hashed since the port; the tavern is untouched (its twin stays `0x86E05F527E54E795`). **NOT here, by ruling:** street combat sheets (9c) and the street Watch (9b) sequence after the justice build. **SKIPPED, said so:** a Deed-style street memory — the SocialLedger keys on tavern actor ids, the ward's id space overlaps it with no mapping, and a hashed WardActor field would be a layout move beyond this declaration. Proved by `native/tests/test_street_panic.cpp` (8 cases, the witness rule computed independently of the sim, the client path included); the number and the gate stamp are recorded in `docs/BASELINE-WORLD-HASH.md` ("Oblivion feel — street panic"). |
| — | Justice build landed: the Daggerfall court, jail and the rope (added 2026-09-11) | **The ruling of 2026-09-02 is BUILT on `justice/build`, sequenced after combat exactly as VETO 4 ordered.** Eli, verbatim: *"If the player is tagged as a criminal it should be like Daggerfall where you can go to court and you can face jail or execution (game over)."* **TWO PLAYER-END PATHS, NEVER CONFLATED, enforced in code and asserted both ways:** combat defeat stays the nemesis ruling (promoted-against, coin taken, quay revive, you live); COURT EXECUTION -- THE ROPE -- is the one true game over and the only place the player ends. **Who presides:** Father Maell at the Mission; the Watch petitions (`Sentence` is what the paper ASKS, Eli's 2026-07-14 ladder verbatim) and the Watch hangs. **The tag:** WANTED (paper), WANTED FOR BLOOD (`murderer_`, cleared only by the bench), CONDEMNED (the rope passed and commuted, never cleared), one HUD row. **The arrest with paper** opens a HEARING on the ledger (hashed, codec v5) instead of a clock jump: the charge sheet is written DRAW-FREE at the arrest, the arrest's one draw is spent where it always was, and the page is DERIVED from the record. **The plea:** I DID IT (draw-free, +6 CONFESSED, never doubled, never spared) or I DID NOT (THE PRIEST IS A MAN, plus or minus ten off bits 16+ of the arrest draw -- same-roll discipline, no new stream; SPARED at 55, DOUBLED under the band and the Mission remembers the lie, temple -8); HEAR THE PAPER shows the sheet as phrases before the plea. **The weighing** is `weighPetition`'s shape one for one (`weighArraignment`: base 24, TONGUE, THE DOOR, THE WARD, TAKEN BEFORE, HEAT, BLOOD, N SAW IT, THE ROOFS, THE SECOND RUNG, THE ROPE ONCE) and is SHOWN in the reference's four-line check block. **COIN BUYS NOTHING AT THE BENCH:** no offering row, no bribe; the fine is a sentence; what you gave at the Mission's door -- temple standing, THE DOOR -- is the only currency the Flame reads. **The judgments** (a new append-only `Judgment`, `Sentence` not extended): SPARED / FINED (`fineFor`, capped at the purse, the shortfall worked off 1 day per 4 Royals to 7) / HELD (the shipped 24..72 h, doubled on a failed denial) / BOUND (5 days, the fine forgiven, temple +4 -- banishment dropped, one district exists) / THE HAND (sparable at 38: the priest overrules the sergeant) / COMMUTED (the rope's mercy: the hand, 12 days, `murderer_` served, `condemned_` forever, recognised at 120 per mille, mercy ONCE -- the next rope hearing has one row, I HAVE NOTHING TO SAY) / THE ROPE. **The rope is the ROPE tier and nothing else:** a witnessed murder (a watchman included) and a Skyrunner's second arrest with paper -- never theft, never repeat violence alone; a witnessed murder is never SPARED. Jail is the shipped `skipHours` widened through what exists: heat cools through it and is then set to `kHeatAfterSentence` (12, live now -- written AFTER the skip), every taken job past its night dies, the ward's roll runs the days and a quarter inside a sentence charges the ground penny (the convict may be the tenant in the OTHER hearing -- allowed, the two courts touching is the design), the roofs warm +4 and the S4 mirror halves it onto the Watch, the body mends over a day. THE NEMESIS DOES NOT RISE from a sentence. **The page** (`render/hearing_page`): the strip-card master/detail shape, TAKEN TO THE MISSION on the arrest's own release (the body to the Mission's arrival tile, the walk elided), the paper laid, the charge off the sheet, the three rows (armed on the first press, confirmed on the second -- the QUIT pattern), the check block, the verdict badge, the sentence in numbers, the priest's word out of fourteen new `court.*` bark tables (a content append beside `watch.*`, the `watch.condemned` precedent: `court.taken` is the officer walking you in, `court.plead` the priest pressing once a plea is armed, the four openings, the seven judgments, the lie; one `court.blood` row nearly says the learned word and no common-folk line does; every row listed for the owner's redline in `docs/frames/justice/README.md`). **The grammar exception, declared:** the hearing has no opener and no back -- ESC closes the paper or disarms a plea and does nothing else; the ten world verbs' `dismissOverlays` cannot touch it; the player cannot walk out of custody (the movement input is dropped in `step()`); PAUSE still opens over it with WAIT refused ("THE PRIEST IS WAITING."). **THE ROPE, staged:** the badge held `kJudgmentHoldSteps`, THE DROP taken by the player's own row, silence (no one-shot -- ThudHeavy is the floor and this is not the floor), the black dip that HOLDS (no fade-out), the plate `HANGED AT THE SALTGATE POST. / BY THE WARD. FOR <the corpse's roster name, or THE SECOND RUNG>. / THE FOURTH DAY. 23:52.`, then `1 - A NEW MAN / 2 - LEAVE` -- no save row because no save exists; A NEW MAN loops `main()` back to the creation window for a fresh run with a clean ledger, LEAVE is the shipped quit. **Resist/escape:** nothing new -- run (the Closing 12 s, the roofs) or swing (Lethal; killing him is the rope). The Wielder at the bench is a seam left open, not built. **The ONE declared tavern/gate-workload baseline move of this build, RE-BLESSED ONCE: `0x86E05F527E54E795` (feel/build) -> `0x837E94019BC49C25` (justice/build), `granadad-twin-gate --tavern --ticks 900`, run A == run B** -- `CrimeLedger::hashInto` carries the v5 court record (the hearing included) and the ledger is hashed under the tavern; the workload never arrests and the draw schedule did not move. The population baseline `0x2646C1AAA2BA38DF` did NOT move (no WardActor touched; the street Watch stays the named ceiling). Proved by `test_court.cpp` (28 cases: the tag, the sheet, the weighing, both pleas, the rope tier only where ruled, mercy once, the v5 codec, the room's arrest and sentence, the two paths never meeting) and `test_hearing_page.cpp` (14: TAKEN, the page pixel-exact and word-exact, the check block, the grammar on keyboard and pad, the rope's plate and rows, A NEW MAN, the HUD tag), and photographed through the real `--court[=wanted|page|paper|plea|deny|serve|rope|newman]` line, `--flame --court=...` for a sheet with THE DOOR on it, and `--creation` for the window A NEW MAN opens (`docs/frames/justice/`, its README the fidelity table). The BARKS & GATE lane gated the final tree (native/ digest `4b790c199f96417cd33cc2530f545be84415d2fe4646903de6e79c9afff1b4f7`: 90/90 ctest entries, 1,197 cases; `verify-windows.ps1` PASS) and read the two numbers off that exe: tavern `0x837E94019BC49C25` (the one declared move, as re-blessed here), population `0x2646C1AAA2BA38DF` unmoved. Ceiling, named: the Watch in the street, the cell as a place, a night in the cell before the bench, the Flame's pierce at the bench, a grace bribe at the Closing stance, NPC criminals before the same bench, the priest's face, a save file and its `THE LAST SAVE` row. |
| — | Justice fix pass: the critic's eleven defects closed, no baseline moved (added 2026-09-11) | **A world-class-director critique of the justice build scored it 7/10 and named eleven defects; every one is closed on `justice/build` and the two numbers did not move.** (1) **The arrest has its moment on screen** before the bench does anything: the officer's own line (`watch.held` / `maimed` / `condemned`) said on the alert row IN THE ROOM with his hand on you and held `kTakenOfficerSteps` (150), then the cut -- black, one line in the rope plate's register, `TAKEN TO THE MISSION. 23:00.`, held `kTakenPlateSteps` (105) -- then the body at the Mission's door and the page. Every key but Pause is swallowed under the beat; `inCustody()` covers it; the crosshair promises nothing. Pinned on the RENDERED FRAME in `test_hearing_page.cpp` (the drawn frame is the plate byte for byte against the plate composed alone over black, and not black alone). (2) **The priest's openings are keyed by the case** and rotate only within the case: `court.paper` (a thief the Mission has never seen) / `court.paper.door` (THE DOOR above zero), `court.blood`, `court.roofs.hand` / `court.roofs.rope`, `court.nothing` -- sixteen `court.*` tables, three rows each, and every row of a table is true of every hearing that table can open (no row names a gift, a rope, a corpse or a night the sheet may not carry; the lie rows never name the sentence's shape; the learned word is never said to a layman, not even nearly -- ruling 8 of the spec is superseded). Every redlined row rewritten; the full list in `docs/frames/justice/README.md`. (3) **The check block is packed by whole terms** (`packTerms`): a row breaks only between terms, so `- 20 THE WARD` never parts from its sign at 640, 960 or 1280; the row of air before the verdict is the one row that gives at the narrowest window. (4) **A night is a whole night**: `cellNights` rounds the draw's 24..72 h to the nearest night ONCE, in `sentenceTerms`, so `TWO NIGHTS` is 48 h on the clock and the release lands on the same clock face (`DAY 3. 23:00.` from `DAY 1 23:00`, not `DAY 4. 00:00.`); the residue itself is untouched (same-roll discipline). (5) The officer's walking-in line leaves a judged page; (6) no `[THE PRIEST WEIGHS]` badge over nothing (mercy once) and no `55 SPARED` on a confession's lines; (7) each tier's consequence literal says only what is on the table (no rope named on a cell or hand charge; `MERCY OR THE ROPE, AND NOTHING ELSE.` / `AND NEVER SPARED.` on the rope's); (8) the short-way `CrimeLedger::arrest()` writes combat/build's bits exactly on the Condemned rung (`condemned_` + `maimed_`, not `commuted_`, `murderer_` untouched) and the bit-for-bit comment is true again; (9) the HUD tag row fits its 34 columns by construction (a commuted man with fresh blood is `WANTED FOR BLOOD`, the riders drop whole, the number is never lost). (10) **The end rows arm before they take** (the QUIT pattern; `-- SURE? <key>` on the tail; ESC/B disarms), which is also what makes the A NEW MAN frame a distinct state. (11) **`--court=` photographs every state a real verb sequence can reach**: `cull` (his line, his hand on you), `taken` (the plate), `armed`, `hand` (the Skyrunners' oath off Finch first, so the paper asks for the hand and I DID IT lands THE HAND -- real play), `bloodtag`, `ropepage` (THE DROP offered, not taken), beside `wanted`, `page`, `paper`, `plea`, `deny`, `serve`, `rope`, `newman` (armed, then taken). SPARED, COMMUTED, the CONDEMNED tag and the mercy-once page need a warmer ward than the smoke's nobody can earn in one evening and are DECLARED GAPS, proved in the suite and not on a frame. `scripts/shoot-court.ps1` shoots the whole set; the stale 640x360 set is deleted. The critic re-scored it 8/10 and left four nits, folded in: the hand tier's check block prints its own lines (`THE LINES: 38 HELD  14 THE HAND`, no FINED on that tier), the rope's sentence row is the post and the hour (`THE SALTGATE POST. 22:00.`) and not an echo of the badge, the pause menu's WAIT row under the arrest beat reads and says `THE WATCH HAS YOU.` (keyed on `inCustody()`), and the frames README points at the script rather than an ignored log. **No baseline moved**: the tavern twin reads `0x837E94019BC49C25` and the population `0x2646C1AAA2BA38DF` off the re-gated exe (run A == run B, both) -- no hashed field changed, the workload never pleads or serves, the rounding lives in a pure function of the record. |
| — | Controls: nine and the sticks, the second clean break of the insert-only enum rule (added 2026-09-11) | **The control scheme is nine world verbs plus M on the keyboard, down from thirteen core verbs and 42 physical inputs.** Eli, verbatim, 2026-09-10: *"I want you to work on simplifying the controls and minimizing the number of inputs necessary. Even a game like Morrowind worked on the console with just a few buttons. When I press LMB I want to enter fighting mode and hit whoever is in front of me."* SWING (MOUSE1 / RT, one press from hands down raises them and swings), GUARD (MOUSE2 / LT), CAST (C / RB), USE (E / A), SNEAK (LCTRL / B), JUMP (SPACE / Y), RUN (LSHIFT / the stick), NOTES (J / D-pad up), PAUSE (ESC / START), and MAP on M. On a pad the ward map is a page of NOTES. The bumpers and brackets page a six-page ring (sheet, chart, letters, casebook, ward map, grimoire), the triggers and TAB step sub-tabs inside a page, T and X are a page's second commit, SELECT is WAIT, D-pad left and right step the quick bar. CUT, with where each went recorded in `CONTROLS-SPEC.md` section 4. The QuickWheel, the page pair as bindable actions, the F1/F2 pages, F3, the walk toggle, the map's pad button, Attack on X and Cast on RT. **THE INSERT-ONLY ENUM RULE TAKES ITS SECOND DECLARED CLEAN BREAK HERE**, exactly as #85 took the first and for the same reason. There are no shipped players and `granadad-controls.cfg` sits beside an executable nobody outside this repo has run. Surviving verbs keep their file names, and `fromText()` migrates the two file shapes that exist on this machine by a stated rule (an old file is one that names a retired verb, and in an old file a slot holding an old shipped key becomes the new shipped key while anything else stands), so no saved binding is silently reinterpreted. The rule resumes the moment this ships. **No baseline moved.** Input and presentation only, no sim field touched. The tavern twin reads `0x837E94019BC49C25` and the population `0x2646C1AAA2BA38DF` off the gated exe, run A == run B. |

| — | The Kit landed: the item record, the load, the worn slots, defence v1 (added 2026-09-12) | **D10 of the Oblivion roadmap is BUILT on `lane/kit`, as ruled: inventory lives on the Character tile; gold stays weightless and is not an item; IN HAND is always printed once the Kit exists; a worn slot prints only when the raws hold an item for it; defence v1 is flat integer DR per worn piece; WardActors carry, drop and yield nothing.** Eli, through the wizard, 2026-09-11: *"THE KIT IS A GO."* **THE RECORD:** `content/raws/items/items.json` is the registry (`sim/items.hpp`, the spellbook's tolerant loader shape, document order): id, name, weight in DRAMS, worth in Royals, slot (hand / body / head / feet / trinket / none -- FEET is the one row past the ruling's four, because the authored set carries boots and a boot is not a trinket), the brawl line's own class as a column (`Weapon` is untouched: `strike()` and `classifyFight` read the class exactly as before), the class's span (an authored span that disagrees with its class is REFUSED at load, so the sheet and the roll cannot lie to each other), DR, heat, and one line in the ward's own register. Twenty-three rows, every one a docker's thing: the cudgel, THE EVICTOR (by the id the eviction case grants), a mace, a bottle, a knife, a dagger, a cutlass, a boat-hook, a coat, a hood, boots, a purse, a lantern, a rope, picks, a letter, the bale, the strongbox (fixed: it has a record so the world can draw it and the prompt can name it, and TAKE refuses it out loud), and the five sack goods. **THE KIT** is dense int32 counts per row (the Stash pattern copied whole: hashed, `encode()`/`decode()` BY ID so an appended raw never moves a saved Kit, an id the raws lost refuses the whole image) plus the equip map, one worn row per slot. **THE SACK STAYS THE ILLICIT VIEW:** the five contraband goods are rows so the sheet prints them, but their counts stay in `CrimeLedger::stash()` (hashed under the ledger, codec v5, read by the Watch's load rule, the impound, the contract board and the radiant work); the picks stay on their counter for the same reason; the Kit never stores either and the tile composes one list out of the three. Folding the sack into the Kit would have re-cut the court's codec and six readers for no feel the player could see -- he sees ONE list either way. **THE HAND IS DERIVED:** `playerWeapon()` is the worn hand row's class when one is worn, else the byte `setPlayerCombat`/the enum grant set (the tests' and drives' way of arming a hand with no item behind it; a direct arming bares the hand row). `grantPlayerWeapon("the_evictor")` puts THE EVICTOR in the Kit and on the hand as a THING: weighed, printed, droppable. **THE LOAD:** every dram on the body (the Kit, the sack, the bale on the shoulder, the picks) against `loadCapDrams(MGT) = 160 + 2 x MGT` -- 240 at the base sheet, the sack's own 240, PROGRESSION-SPEC section 5's `300 + 20 x MGT` in this build's units, off the EFFECTIVE sheet so a held Might tuning carries more. TAKE refuses past it with the numbers on the row (`TOO MUCH ON YOU ALREADY. 238 OF 240 DRAMS.`); the legs slow through `loadSpeedScaleQ8` (256 to half the budget, down to 128 at it) composed with AGI's gait at the one movement seam (`Session::step`, `PlayerBody::setSpeedScaleQ8`) -- player only, integer, never a WardActor. **DEFENCE v1:** the sum of every worn piece's DR comes off a landed blow AFTER the guard has argued, floored at one point through any coat (blockedDamage's own floor, for its reason: armour that nulls a blow turns the fight OFF), at the ONE place a blow lands on the body (`stepBrawl`); HARNESS trains on a blow the kit softened; the row says `COAT TURNS 2.` once per blow. COMBAT-SPEC's coverage-weighted AC and per-part wear stay the v2 ceiling. **THE VERBS, through the existing interact chain and the Menu's own grammar, no new key:** the crosshair names the thing (`TAKE  ROPE  48DR`, `TAKE QUIETLY` crouched -- the stance moves who notices, never the verb) and says THEIRS in its own Owned accent (the reference's red hand) BEFORE the press when taking it is theft (the same three-clause witness rule, `Crime::Lift`, `Deed::Robbed`, the house's own offence); a thing on the ground outranks the street's people (a mouse was TALK over a rope) and a named lead outranks the thing (the investigation first); a Dead roster body in reach is `SEARCH  <NAME>  DEAD` when no living body is nearer, and the press opens his kit -- the authored corpse kit for his name, then his role, less what was taken (a hashed mask), TAKE ALL as the last row -- on the Grimoire widget (`grimoireOpen_` with a corpse id: no fifteenth page flag); the dead do not witness the taking and nothing in the ward's law names it (v1). On the Character tile: IN HAND, the worn slot rows (`ON THE BACK  COAT  DR 2`, `AT THE BELT  NOTHING`), `LOAD  151 / 240 DRAMS`, then every carried row with its weight and worth and its IN HAND / WORN / SLOT n / HOT marks; ENTER wears or bares the row, LEFT/RIGHT walk its quick slot (the Grimoire's own cycle: a slot is a spell OR an item, tagged `item:<id>`, and the strip and the number key read either), X drops it at the feet. **THE WORLD DRAWS IT:** `PieceRole::Item` and the catalogue's `items` table (`content/raws/world3d/docks-pieces.json`: one Synty static per item id, a point-up weapon pitched flat) -- `render3d/ground_items.cpp` writes the room's hashed ground list, the four strongboxes and the snug's bale as instances per frame after the placed pieces; the renderer never removes one (TAKE does, in the sim); the placeholder rule holds (no file, no draw, the sim untouched). **THE ONE DECLARED tavern/gate-workload baseline move, re-blessed ONCE: `0x837E94019BC49C25` (justice/build) -> `0x63F354D02A6B600F` (lane/kit), `granadad-twin-gate --tavern --ticks 900`, run A == run B** -- the Kit's counts and worn rows, the ground list, the corpse masks and the turned-blow counters joined `Tavern::hash_into`; the workload never takes, drops or wears and the draw schedule did not move. The population baseline `0x2646C1AAA2BA38DF` did NOT move (no WardActor touched; the street carries nothing, by ruling). **A corpse-convention fix found by the line, folded in:** the door's responder was replaced only when `Downed`, so a bouncer the player had KILLED was stood back up by the ladder's own `setActivity` and could be talked to; the responder, the on-duty pick and the vermin loops read `isFloored` now. **NOT done, said so:** attributes deriving from skills (D10's last clause) -- every runtime reader of MGT/AGI/VIG/WIT and every pinned pool, gait and damage number moves with it, so it is its own gated pass; the load budget already reads `effectiveAttributes()` and follows the day it lands. Proved by `test_kit.cpp` (17 cases), `test_kit_page.cpp` (6), the rewritten `test_character.cpp` pins (24 rows bare, 25 armed) and the `--kit[=take|theirs|sheet|equip|slot|dr|search|load|drop]` line in `test_scripted_lines.cpp`; photographed through that line into `docs/frames/kit/` (`scripts/shoot-kit.ps1`), its README the fidelity table. |

| — | Street senses, leg (a): the crowd flees and cowers for real (added 2026-09-11) | **The first leg of the STREET SENSES move (roadmap item 13, Eli's consented one-move-three-legs) is BUILT, and the population baseline is DECLARED MOVED and RE-BLESSED for the leg: `0x2646C1AAA2BA38DF` -> `0xF493AF6F939D52D3` (`lane/street`), `granadad-twin-gate --population --population-hour 16 --ticks 7200`, run A == run B, two invocations, 19,947-byte report.** Eli, through a wizard, 2026-09-11: CONSENT to the ONE declared move of the population baseline, ALL THREE LEGS, re-blessed per leg with a violence leg in the gate. **9a shipped the alarm and the panic recovery; this completes it so the reaction is legible and driven by the player.** What landed: **(1) `WardPopulation::setPlayer(x, y, band)`** -- the mirror of `Tavern::setPlayer`, pushed by the client every step (`Session::step`, the same block that fires `alarm()`), hashed, so a frightened body reads WHERE THE PLAYER IS; **(2) FLEE IS AN AWAY PLAN** -- `actFlee` steps directly away from the pushed player (the orthogonal step that opens the most Chebyshev distance, draw-free, deterministic tie-break), leash ignored, falling back to 9a's drawn shuffle only when no player is pushed (the gate before its assault, a beast's panic) or the body is boxed -- so the no-player arithmetic is byte-for-byte 9a's; **(3) COWER, appended after Hunt** (`WardPolicy::Cower = 7`, the Hunt ordinal precedent, hashed byte) -- the same FLEE gate, the other response, for the types the gazetteer's crowd ladder stands rather than runs (`wardTypeCowers`: Shopkeeper, PriestOfTheFlame, DiscipleOfTheFlame -- "Serfs flee, Shopkeepers bucket-chain, Priest walks in"), draw-free, holds the tile and faces the fright; **(4) the loiter shuffle is kept DIRECTION-BLIND** (`oneDrawnStep`, shared) so a body merely standing near the player does not back away from him -- the away-vector is a frightened body's alone. **THE VIOLENCE LEG IN THE GATE:** the population workload registers a `StreetAssaultDriver` (TickBegin, before the Actors phase) that throws a scripted blow on the Tarwalk at 16:30 -- `setPlayer` + `alarm(Blow)` at the first standing docker's tile, held for a window -- so the number the baseline is taken from PROVES the behaviour (the report now shows `flee=`/`cower=` scattering), where 9a's player-less run moved nothing. **Numbers per D5, unchanged from 9a and pinned:** panic radius by severity (Steel 6 / Blow 12 / Kill 24 tiles), same band, line of sight; recovery `kPanicRecoverPerTick` 9/s, so the measured panic is ~31 s (Steel), ~78 s (Blow, in D5's 60-120 band), ~105 s (Kill, in band) -- Steel is deliberately sub-60 as a pre-blow warning, and `test_street_panic` pins all three. **The one declared hashed field is the player position** (`playerX_/Y_/band_/playerKnown_`, appended to `hash_into`), constant zero in any run that never pushes a player. **THE TAVERN TWIN DID NOT MOVE: `0x837E94019BC49C25`** (`--tavern --ticks 900`, run A == run B) -- no hashed tavern field touched. The nemesis arc is untouched (no combat/tavern change; `test_nemesis` re-read, unaffected). Legs (b) STREET BODIES (a hittable WardActor combat sheet, the player swing seeing `people_`, the murder-law path) and (c) THE WATCH (MilitiaWatch Pursue + arrest through the hearing seam) are the NAMED CEILING of this leg and the next declared moves, sequenced on the same branch. Proved by `test_street_panic.cpp` (the split by type, the away-vector, the pinned panic length, the twin run) and photographed through the real `--street-assault` verb (`docs/frames/street/`). Files: `ward_actors.hpp/.cpp` (`Cower`, `wardTypeCowers`, `setPlayer`, `actFlee`/`actCower`/`oneDrawnStep`, the hash), `session.cpp` (the one client push + the `--street-assault` capture drive), `gate/workload.cpp` (`StreetAssaultDriver`). |

| — | Street senses, leg (b): a body to hit (added 2026-09-11) | **The second leg of the STREET SENSES move is BUILT, and the population baseline is DECLARED MOVED and RE-BLESSED for the leg: `0xF493AF6F939D52D3` -> `0xED0CA90E26DB0F5B` (`lane/street`), `granadad-twin-gate --population --population-hour 16 --ticks 7200`, run A == run B, two invocations, 21,746-byte report.** The gap analysis' first finding: "a swing on the Tarwalk hits nobody because the sightline raycast walks the 17-body tavern roster only; WardActor carries no hp, no downed-by-a-blow, no dead-by-violence state." **What landed, and the one rule kept whole:** **(1) THE COMBAT SHEET on `WardActor`** -- `hp` (kActorHealth, the Gull's own twenty-four), `downedUntil` REUSED as the brawl floor (the prey's revive latch is the same absolute-tick shape), `slain` (dead by violence: sets `dead` too, so the corpse frees its tile, never ticks and never stands, and the census tells a killing from a famine), `fightUntil` and `swingSeq` for a body that swings back -- all appended to `hash_into`. **(2) THE SAME RAY, THE OTHER ROSTER** -- `WardPopulation::sightlineTarget` casts VETO 1's projection line for line (along = (fx*dx+fy*dy)>>16, perp = (-fy*dx+fx*dy)>>16, on the line iff 0 < along <= kMeleeReach and \|perp\| <= kBodyHalfWidth, smallest along, ties to the lower id; a body's Q8 position is its tile centre) over the district's standing persons on the player's band, and `Session::attackUp` -- the one client hook in the attack path -- PICKS THE NEARER ALONG of the Gull's target (`Tavern::playerSightlineAlong`) and the street's. `test_street_bodies` computes the projection independently and requires agreement. **(3) THE SWING IS THE ROOM'S** -- `Tavern::playerAttackUpStreet` shares `armPlayerSwing` with the roster swing (ONE charge tier, ONE winded refusal, ONE recovery, ONE wind cost, factored rather than copied), spends THE ONE ROLL (`drawForPlayerAction`, the same stream position the roster swing would have spent it) in `strike()` on the street body's sheet, classifies the fight with that fighter IN it (`classifyFight`; the Subdue floor and the Lethal rules unchanged: fists on a fresh docker is a brawl, the player's steel, a Harm swing on a bloodied man, or Kill intent flips it), applies intent-by-verb, lands the deed on the social ledger under `kWardSpeakerIdBase + wardId`, reports the offence, and a KILLING (lethal, downed, not crowned -- the crowned Evictor blow still only puts a man out) goes through the murder law exactly as `slayActor` does: `Deed::Slew`, the room's witness spread, and `CrimeLedger::markMurderer(witnesses)` on the count the STREET took BEFORE the body dropped (`witnessesInSight`: the three-clause rule at `kWatchSightTiles`, the Watch included, the victim excluded). The tavern roster is never touched; nobody is promoted onto it. **(4) THE STREET TAKES THE ANSWER** -- `applyStreetBlow` writes the sheet back and: downed under BRAWL is the floor (`kStreetFloorSeconds` 6, off the board, the tile freed, `standUp` at `kStreetStandHp` a quarter, on his own tile or the first free neighbour, else he lies a little longer -- the den-full rule); downed under LETHAL is death; a struck `wardTypeFightsBack` type (Sailor, Thief) starts SWINGING BACK (`fightUntil` 20 s, `WardPolicy::Brawl = 8`, appended); anybody else struck ROUTS through the leg (a) flee plan at `kStruckPanicFloor` (the Kill floor, ~105 s -- deeper than the ~78 s of a bystander who only saw it); and the crowd is alarmed at the tile (Kill for a killing, Blow for a blow -- a bar-fight punch is the house's, a docker beaten in front of the fish market is not). **(5) THE STREET SWINGS BACK, ON THE PLAYER'S OWN SHEET** -- a brawling body closes on the pushed player (the route step every plan walks) and, in reach, draws ONE roll per swing on its own key and sequence (`context.draw(id ^ 'SWNG', swingSeq)`, the Gull's `npcSwingSeq_` shape, no shared index, no new stream) into a MAILBOX (`takeStreetBlows`, unhashed by design: produced in tick(), consumed the same step, drained by the gate's driver); the client lands each on the player through `Tavern::takeStreetBlow` after its own Q8 reach check (a player who stepped back is whiffed at, never cancelled on): the HARD band carved off the thrower's roll on the NPC swing's own bits, the held guard softening it (blockedDamage, shieldwall, the catch wind, a hard swing breaking the guard), the floor by class, the hands up, and the defeat seam with NO roster winner (floored, the quay revive, no rise -- a street winner's nemesis rise is ceiling, the book keys on the roster). The lull that lowers the hands also resets intent-by-verb, since a street fight has no brawler list to empty. **(6) THE READING** -- `slainName()` names a street corpse through the district's identities (the ledger's `kWardSpeakerIdBase` row, the last in id order) and `slainOnStreet()` says where, so the charge sheet reads `THE WARD SAYS YOU PUT <docker> DOWN IN THE STREET. N SAW IT.` and the rope plate `FOR <docker>.`; a floored street body is drawn where he fell on both renderers (the Gull's Recover and Death clips on the 3D rig, the flat Downed quad on the billboards). **THE GATE'S VIOLENCE LEG** now strikes the docker it found: `strike()` on his sheet a fist a tick on the driver's own salt until he drops, then leaves him to lie his six and STAND -- `downed=` rises and falls on the compared report, `brawl=` and `slain=` printed beside it. **Same-roll accounting:** the player's street swing spends drawForPlayerAction once (the roll the roster swing would have spent); a street body's swing-back is one draw per swing on its own key (as the Gull's NPC swing is), with the hard band carved off it; the sightline, the witness count, the floor, the stand-up and the rout are draw-free; the gate driver's fist is its own system-salted draw standing in for the player's. No new RNG stream. **THE TAVERN TWIN DID NOT MOVE: `0x837E94019BC49C25`** (`--tavern --ticks 900`, run A == run B, twice) -- `armPlayerSwing` is a pure factoring of the roster swing (same operations, same order), the new methods are never called by the tavern workload, and `slainName`'s fallback reads only when a ward memory exists. `--nemesis` 7/7 re-read. Proved by `test_street_bodies.cpp` (the same ray computed independently, down and up, rout and swing-back, the crowd, the player-side landing, a street kill's murder with the witnesses counted through the client's own attack path, the twin run) and photographed through `--street-assault=blow\|down\|up\|kill\|hearing` (`docs/frames/street/`). Ceiling, named: a street winner's nemesis rise; beasts on the street's ray; a guard and a wind-up on a street body (the Gull's rhythm). |

| — | Street senses, leg (c): the Watch on the beats gets eyes (added 2026-09-12) | **The third and last leg of the STREET SENSES move is BUILT, and the population baseline is DECLARED MOVED and RE-BLESSED for the leg, completing all three: `0xED0CA90E26DB0F5B` -> `0xDD3890042DD2B472` (`lane/street`), `granadad-twin-gate --population --population-hour 16 --ticks 7200`, run A == run B, two invocations, 22,320-byte report.** The gap analysis' third finding: "THE STREET WATCH IS SCENERY. Thirteen MilitiaWatch WardActors by day and seven by night walk beats with no eyes." **What landed:** **(1) `WardPolicy::Close = 9`**, appended (the Brawl precedent, hashed byte) -- a watchman with cause and a clock (`closingUntil`, `closeCause`, `sheatheBy`, all appended to `hash_into`) walks the pushed player down, priced under Brawl (a blow on him now makes him a brawler -- `wardTypeFightsBack` gained MilitiaWatch -- and he fights rather than arrests) and over everything else. **(2) `alarm()` stopped skipping the Watch outright** -- a watchman is given cause by the same three-clause notice rule (own sight range at `kWatchSightTiles`, line of sight, never for a presented Wielder, D6 absolute deference unchanged) everybody else's fright already reads, closes, and ARRESTS AT REACH through the ONE seam the Gull's Cull uses: `Tavern::arrestByStreetWatch` calls a newly shared `arrestPlayer` factored line-for-line out of `applyArrest`'s own body (one charge sheet, one seizure, one hearing path, two officers). Steel alone is a DEMAND first (`street.sheathe`, `kSheatheGraceSeconds` 6 s), an ignored one an OFFENCE (`street.offence`, `kSheatheOffenceHeat` 10 heat, no arrest by itself) -- D5. A struck body that is not a fighter and not the Watch says `street.routed` once. Four new authored keys in `content/raws/barks/street_barks.json`, falling through to the Gull's own `watch.*` rows until authored. **(3) THE REAL BUG THIS LEG SURFACED AND FIXED: a house's own brawl is not street business.** The gap analysis' bar-fight exemption ("a brawl-class fist fight is the house's law... the Watch has no cause in it") only ever existed for Watchman Cull's own separate, already-nuanced watch. The moment the street Watch stopped being unconditionally skipped, `Session::step()`'s PRE-EXISTING per-step alarm (leg (a); room HP falling under an escalated fight, unconditional on indoor/outdoor since it shipped) started reaching a street watchman through the Gilded Gull's own open door -- the owner's rule the crowd already leans on ("a killing at the bar reaches the Tarwalk only through the door"), now also read by a beat cop with no jurisdiction over a sanctioned house brawl. Caught by `test_scripted_lines.cpp`'s nemesis line: a watchman closed on the player mid-brawl and arrested at reach, clearing the room's brawl roster (`arrestPlayer`'s own `brawlers_.clear()`) and stalling the arc at its third win on every one of the six walk-length timelines that line holds itself to -- a hard, deterministic regression, traced with temporary stderr instrumentation at the alarm site, the watch-cause site, the arrest site and the fight loop itself rather than guessed at (two speculative fixes -- a line-of-sight check on `WardPopulation::sightlineTarget`, and gating `Session::attackUp`'s street-vs-room target pick on `Tavern::playerInside()` -- were tried first, produced BYTE-IDENTICAL failing output before and after, and were fully reverted as confirmed dead ends; `onWalkingGround` was tried third and also failed, because the Gull's own floor tiles satisfy it exactly as the street does). **The fix is a fourth hashed field**, `playerIndoors_` (`WardPopulation::setPlayerIndoors`, pushed from `Session::step` beside the existing deference push, off `Tavern::playerInside()`, the exact shape of `playerWielder_`): while true, no watchman is ever given cause and one already closing stands down. The ordinary crowd's own door-leak panic (leg a/b, already shipped and tested) is untouched, because only the Watch's branch in `alarm()` reads the new flag. **THE TAVERN TWIN DID NOT MOVE: `0x837E94019BC49C25`** (`--tavern --ticks 900`, run A == run B, twice) -- `arrestPlayer` is a pure factoring of `applyArrest`'s own body (same operations, same order) and the tavern workload has no player and never calls it. **THE NEMESIS ARC, THE VERY THING THIS LEG NEARLY BROKE, IS THE SHARPEST PROOF IT DID NOT**: `granadad.exe --nemesis` re-read on all six of the README's own walk lengths (0/40/50/80/100/120) reads `beats=7/7 mask=127`, `Tarn Wrenhale x3`, `Foreman of The Ropewalk Gang (7 members, toll 6%) holds THE GULLET` on every one -- the exact leg (b) baseline. **Street senses is now complete, all three legs declared and re-blessed.** Proved by `native/tests/test_street_watch.cpp` (a watchman who sees a blow closes, halts, and arrests at reach through the Gull's own seam; steel is a demand with a grace and only an ignored one becomes an offence, never an arrest by itself; a presented Wielder is never given cause and standing one down stands every closing watchman down; the population twin-runs byte-identical under all three) and photographed through `--street-assault=watch-halt\|watch-sheathe\|watch-offence` and the leg (b) `blow` ending re-shot under this tree (`docs/frames/street/`). Files: `ward_actors.hpp/.cpp` (`Close`, `alarm()`'s Watch branch, `setPlayerIndoors`, the hash), `tavern.hpp/.cpp` (`arrestByStreetWatch`/`noteStreetOffence`/the shared `arrestPlayer`), `session.cpp` (the `setPlayerIndoors` push, the client's halt/demand/offence/routed lines, the `--street-assault=watch-*` capture drives), `content/raws/barks/street_barks.json`. |

Spec documents (produced by the gameplay spec track, live in this folder):
- `COMBAT-ACTION-SPEC.md` — the BUILT action-combat model (spec + veto pass assembled): sightline raycast, in-world lethal rules, death, murder hook, the build-lane contract
- `CONTROLS-SPEC.md` — the BUILT control scheme, nine and the sticks: the nine verbs on both devices, the page grammar and the ring, fighting mode from the controls' side, the cuts, the settings-file migration
- `JUSTICE-SPEC.md` — the BUILT Daggerfall-inspired court: the criminal tag, the arrest that goes to the Mission, the plea and the shown weighing, the sentence branch in integers, the rope as the game's one end, the hearing page, the rulings folded
- `COMBAT-SPEC.md` — defense math, hit location, durability, worked examples, unit-test list (the v2 ceiling)
- `PROGRESSION-SPEC.md` — skill list, use-XP formulas, attribute interplay, exploit seams
- `COMBAT-SCREEN-SPEC.md` — encounter transition, screen layout, action economy, UI
- `FACES-SPEC.md` — face file formats, generator part model, determinism, examples

Sequencing: specs first (this track), implementation lands as the G-milestones after the
sim-core F-milestones provide the world the game plays in. The "no gameplay in v0" scope
contract applies to the SIM core's F1-F8; the G-track builds the game layer on top of it.
